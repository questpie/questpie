/* oxlint-disable */
// AUTO-GENERATED by questpie codegen — DO NOT EDIT
// Regenerate with: questpie generate

// ── Routes ────────────────────────────────────────────
import _route_sandbox_rpc from "../routes/sandbox/rpc";

// ════════════════════════════════════════════════════════════
// TYPES — composed from typeof references (zero inference cost)
// ════════════════════════════════════════════════════════════

import type { RouteDefinition, RouteParamsFromKey } from "questpie/types";

export type SandboxCollections = Record<never, never>;

export type SandboxChannels = Record<never, never>;

export type SandboxGlobals = Record<never, never>;

export type SandboxJobs = Record<never, never>;

export type SandboxRoutes = {
	"sandbox/rpc": typeof _route_sandbox_rpc extends { __brand: "route" } ? RouteDefinition<unknown, unknown, RouteParamsFromKey<"sandbox/rpc">> : typeof _route_sandbox_rpc;
};

export type SandboxFieldTypes = Record<never, never>;

// ════════════════════════════════════════════════════════════
// MODULE DEFINITION — static plain object
// ════════════════════════════════════════════════════════════

export type SandboxModule = {
	name: "questpie-sandbox";
	routes: SandboxRoutes;
	collections: SandboxCollections;
	channels: SandboxChannels;
	globals: SandboxGlobals;
	jobs: SandboxJobs;
	fieldTypes: SandboxFieldTypes;
	messages: Record<never, never>;
	services: Record<never, never>;
	emailTemplates: Record<never, never>;
	migrations: readonly unknown[];
	seeds: readonly unknown[];
};

const _module: SandboxModule = {
	name: "questpie-sandbox" as const,
	routes: {
		"sandbox/rpc": _route_sandbox_rpc,
	} as SandboxRoutes,
	collections: {},
	channels: {},
	globals: {},
	jobs: {},
	messages: {},
	services: {},
	emailTemplates: {},
	migrations: [] as const,
	seeds: [] as const,
	fieldTypes: {},
};

export default _module;
