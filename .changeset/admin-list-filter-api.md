---
"@questpie/admin": patch
---

Add declarative list view configuration: `defaultFilters`, `quickFilters`, and `FilterBuilderPanels` prop for controlling which view-options tabs are visible. Fix system field (updatedAt/createdAt) support in `defaultSort`. Recurse sidebar access filtering into nested sections.
