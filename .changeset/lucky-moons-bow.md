---
---

Fix the tanstack-barbershop example live preview flow for barber detail pages by using stable slug preview URLs, enabling draft-aware loading, and wiring preview field click-to-focus support.

No published package versions are bumped because this change only affects an ignored example workspace.
