---
"questpie": patch
---

Add the experimental `questpie/channels` server primitive with typed wire params, immutable channel definitions, authorization and presence gates, Zod-validated publishing, and request-bound `ctx.channels` access.
