---
"questpie": patch
---

Teaching layer for the 3.6.0 batch (P7) + the infer-first map (P0 docs). The skill and docs now teach the two auth-context facts jubli had to dig out of dist: `getContext<App>()` works inside Better Auth callbacks (`/auth/*` is a raw route executed inside `runWithContext`), and a bare `{ accessMode: "system" }` CRUD context elevates only the mode while session/db/locale inherit from the request scope. New `references/type-inference.md` + `docs/backend/type-inference` page map every type need to its sanctioned inference one-liner (per-op `ctx.data`/`ctx.input` access typing, `AccessContext` helper params with the verified return-annotation cycle cut, `CollectionDoc`, `InferRoute*`/`InferJobPayload`, manual key-registry augmentation). Recipe batch closes every audited skill-coverage zero: sandbox/executor reference (`ctx.executor.run` — also corrects the docs code-execution page, which taught a fabricated `sandboxModule`/`ctx.sandbox` surface), job-level cron (`options.cron`), pgvector + embedding providers, `resendAdapter`/`plunkAdapter`, module-config merge helpers, realtime topic builders, `mcpPlugin`/`openApiPlugin` double-registration warnings, raw-route webhook/streaming recipes (fixing a fabricated `route({ method })` API), signed URLs, `withTransaction`/`onAfterCommit`, and client-side `authClient`. New `scripts/skill-coverage.ts` + `skills/questpie/coverage.json` gate makes coverage computable: it enumerates the public export surface, greps the skill for every symbol/concept, checks docs-nav integrity, and fails on zeros in `--strict` mode (landing in warn mode).
