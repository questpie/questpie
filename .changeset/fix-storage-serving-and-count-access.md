---
"questpie": patch
---

Infer common media MIME types from filenames when storage metadata is generic, emit safe ASCII Content-Disposition fallbacks, and reuse prechecked read access for realtime count snapshots.
