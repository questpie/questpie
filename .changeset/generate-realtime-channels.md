---
"questpie": patch
---

Discover typed realtime channels through the core file convention, merge module and app channel definitions into `QuestpieConfig`, emit them in generated app types, and add `questpie add channel` scaffolding.
