# Database
DATABASE_URL=postgresql://toy_factory_backend:toy_factory_backend@localhost:5544/toy_factory_backend

# Application
APP_URL=http://localhost:3000
PORT=3000

# Auth (Better Auth)
BETTER_AUTH_SECRET=toy-factory-dev-secret-change-me

# Email Adapter (console | smtp)
MAIL_ADAPTER=console

# SMTP (if using smtp adapter)
# SMTP_HOST=localhost
# SMTP_PORT=1025
