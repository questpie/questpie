# Database (Only external dependency!)
DATABASE_URL=postgresql://cityportal:cityportal_dev_password@localhost:5433/cityportal

# Application
APP_URL=http://localhost:3001
PORT=3001

# Auth (Better Auth)
BETTER_AUTH_SECRET=your-secret-key-change-in-production

# Email Adapter (console | smtp)
MAIL_ADAPTER=console

# SMTP (if using smtp adapter - MailHog service in docker-compose)
SMTP_HOST=localhost
SMTP_PORT=1025

# Storage (local | s3 | r2)
# STORAGE_DRIVER=local
# STORAGE_PATH=./uploads

# For S3/R2 storage:
# STORAGE_DRIVER=s3
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret
# AWS_REGION=us-east-1
# AWS_BUCKET=your-bucket
