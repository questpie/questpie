# Team Support Desk QUESTPIE v4 DX evidence

- Status: closed reference evidence from one production-like application
  consumer
- Consumer: `fixtures/team-support-desk`
- Rule: every row points to real application code or an exact blocker; this is
  not a speculative syntax backlog

| Application job                                                           | Current public-v4 spelling                                                                                                                                                                                                                                                                                                                    | Workaround or exact blocker                                                                                                                                                                                                                                                                                                                                           | Proposed deeper framework seam                                                                                                                                                                                                                                                                               | Estimated handwritten boilerplate removed                                                                                                                                                       |
| ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Search tickets by human text                                              | The app authors an exact `tickets.searchByReference` structural Query because foundational Query text Fields expose exact/range comparisons only.                                                                                                                                                                                             | `like`, case-insensitive matching, regex, and full-text are explicitly deferred. The browser offers exact reference search and does not use raw SQL or client-side filtering as an authorization path. The Accepted Search Projection contract is not yet exported by production compiler/runtime packages, so full-text is a non-blocking absence for this vertical. | Implement the already Accepted-but-unimplemented Search Projection seam: committed candidate keys followed by one bounded, currently authorized source plan and cursor universe. Do not broaden foundational Query operators ad hoc.                                                                         | About 45–70 app lines for a future search document/index projection and exact-reference-only UI copy; more importantly removes an unsafe temptation to duplicate Policy after a search backend. |
| Filter a cursor page by optional status/team sets                         | Current production `query.parameter` exposes required scalar UUID/text/integer plus nullable cursor; it does not expose the Accepted bounded scalar-list parameter or nullable scalar filter. The app authors `tickets.list`, `listByStatus`, `listByTeam`, and `listByStatusAndTeam`.                                                        | Four otherwise parallel structural Query/semantic Query pairs and a browser dispatch switch. This preserves SQL Policy/pagination and avoids client filtering. It is the chosen app workaround, not a new Query syntax proposal.                                                                                                                                      | Finish the already Accepted scalar-list parameter contract through public authoring, compiler artifact/binding, PostgreSQL runtime, generated exact types, and cursor scope. One bounded list per dimension lets the empty or full UI state remain explicit without nullable predicate magic.                | Roughly 95–125 handwritten application/browser lines across three duplicated plans, wrappers, codecs, and dispatch branches.                                                                    |
| Let an ordinary Job read current Ticket state or record a business result | Current generated `JobContext` carries root facts, `run`, and `attempt`, but no `data`, Query, Mutation, Action, or Services capability. Team Support Desk therefore passes an immutable minimal ticket/SLA snapshot in accepted input and keeps the Job result as an Operational Fact inspected server-side.                                 | The Job cannot refresh the Ticket or publish user-visible status without stepping outside the public interface. This vertical does not add a seventh status Collection or call raw SQL/global `fetch`; browser Job control remains intentionally absent.                                                                                                              | Complete the already Accepted closed Job checkpoint projection for named generated Mutation/Action commands, with stable command identities and recovery. If ordinary read-only Query callers are desired, decide and prove that narrower capability separately instead of leaking RootExecution Operations. | About 25–40 lines of snapshot input/output plumbing today; user-visible durable status remains an exact blocker rather than a claimed line saving.                                              |
| Name generated operation result types in browser application code         | [`tracer/browser/questpie.ts`](../../../../fixtures/team-support-desk/tracer/browser/questpie.ts) derives every result from `#questpie/client` with nested `Awaited<ReturnType<SupportDesk["queries"][...]>>` expressions.                                                                                                                    | The aliases are truthful and do not duplicate the wire contract, but they are noisy, hard to discover, and must be handwritten for every result consumed by more than one component. No manual response interfaces were introduced.                                                                                                                                   | Generate stable named input and result aliases alongside each Query, Mutation, and Action method, while retaining the callable methods as their single source of truth. This does not require a React adapter or a new client runtime interface.                                                             | About 15–25 lines per modest browser app, plus lower cognitive load at every component boundary.                                                                                                |
| Keep React views current after generated-client calls                     | [`tracer/browser/app.tsx`](../../../../fixtures/team-support-desk/tracer/browser/app.tsx) owns `useState` records, request-generation guards, explicit loading/error state, and calls `selectTicket` plus `loadQueue` after each successful Mutation.                                                                                         | The generated browser client has callable and watchable Queries but no framework-neutral cache integration or mutation invalidation facts. The app deliberately exposes this friction instead of inventing `useQuestpieQuery`, adding a query library, or trusting stale local projections.                                                                           | Expose generated operation identity and invalidation metadata through a framework-neutral reactive cache seam. A separate adapter could later bind that seam to React; it is out of scope here.                                                                                                              | Roughly 90–130 lines of request racing, loading/error state, refresh fan-out, and repeated post-Mutation orchestration in this app.                                                             |
| Separate application transport from browser fixture control               | [`tracer/browser/fixture-control.ts`](../../../../fixtures/team-support-desk/tracer/browser/fixture-control.ts) uses manual `fetch` only for the signed local persona and test-phase report. [`tracer/browser/questpie.ts`](../../../../fixtures/team-support-desk/tracer/browser/questpie.ts) creates the generated application client.      | The browser needs a fixture persona bootstrap and a way to report Firefox completion, neither of which is application data. Keeping them in a plainly named control module prevents those local endpoints from becoming an accidental parallel Query/Mutation contract.                                                                                               | No framework seam is requested for the reporter. A production authentication integration seam may eventually supply `AppContextInput`, but that authority is outside this reference app and must not be inferred from a fixture cookie.                                                                      | Zero application boilerplate claimed; the separation removes ambiguity and makes the two permitted manual fetches auditable.                                                                    |
| Resolve a tenant Membership from Organization and Principal               | [`src/execution.ts`](../../../../fixtures/team-support-desk/src/execution.ts) requires both `organizationId` and `membershipId`; Context resolution can only `bootstrap.get` the Membership by key and then compare its Organization and Principal.                                                                                           | The browser session endpoint and signed webhook payload must carry a Membership id that is logically derivable from the unique `(organizationId, principalId)` membership. Dropping it would require an unauthorized pre-Context lookup or raw SQL, so the redundancy remains explicit.                                                                               | Add a bounded, Policy-independent bootstrap lookup by declared unique key, with exact select and nondisclosing failure semantics. It must remain a Context bootstrap primitive, not expose general Query execution before Tenant exists.                                                                     | About 8–15 lines in each context producer plus one redundant external field per request.                                                                                                        |
| Supply deployment configuration to Services and Routes                    | [`src/auth.ts`](../../../../fixtures/team-support-desk/src/auth.ts), [`src/notification-action.ts`](../../../../fixtures/team-support-desk/src/notification-action.ts), and [`src/inbound-webhook-route.ts`](../../../../fixtures/team-support-desk/src/inbound-webhook-route.ts) use committed, visibly local-only secrets and receiver URL. | Public Service and Route declarations have no application-config injection seam. Environment reads inside Definitions would make build-time evaluation impure, while globals or fixture imports would hide deployment authority. The reference app therefore commits non-production values and documents that it is not a deployment pattern.                         | Provide a declaration-safe configuration capability whose schema is compiled but whose values are supplied when creating the generated app, then project exact typed values into Service factories and Route helpers without admitting ambient environment access.                                           | Roughly 15–30 lines here; the correctness value is larger because real deployments stop copying secrets or URLs into declaration source.                                                        |
| Wait against an absolute business deadline inside a Job attempt           | [`src/ticket-sla-follow-up-job.ts`](../../../../fixtures/team-support-desk/src/ticket-sla-follow-up-job.ts) reconstructs wall time from `performance.timeOrigin + performance.now()` and races a timer with `ctx.signal`.                                                                                                                     | `JobContext` exposes attempt number, heartbeat, signal, and deadline, but no attempt clock. `Date.now()` is structurally forbidden and the monotonic reconstruction is correct for this process-local wait yet distracts from the durable behavior being demonstrated.                                                                                                | Expose the Runtime-owned attempt clock through a narrow read-only `ctx.attempt.now()` or equivalent execution fact, preserving deterministic ownership and cancellation. Do not add workflow scheduling semantics.                                                                                           | About 8–12 lines per time-aware Job and removes an easy source of accidental forbidden ambient-clock use.                                                                                       |
| Express a Field that no caller may supply                                 | The Membership, Team, and Ticket Policies spell denial as `query.not(query.always())`, for example [`src/tickets/policy.ts`](../../../../fixtures/team-support-desk/src/tickets/policy.ts), while Collection Operations derive the values from `operationTime`.                                                                               | The expression is semantically exact but reads like an optimizer exercise in the most security-sensitive map. A local helper would merely rename the same awkwardness and fragment the public Policy vocabulary.                                                                                                                                                      | Add a foundational constant-false expression such as `query.never()` with the same normalized AST identity as `not(always())`; this is a small vocabulary seam, not a Collection or Query syntax redesign.                                                                                                   | Three expressions in this app; effectively zero runtime code, but clearer Field-authority review.                                                                                               |
| Set transition-derived lifecycle values without granting caller authority | [`src/ticket-mutations.ts`](../../../../fixtures/team-support-desk/src/ticket-mutations.ts) supplies `closedAt: ctx.operationTime` or `null` in the authorized Collection patch, and [`src/tickets/policy.ts`](../../../../fixtures/team-support-desk/src/tickets/policy.ts) consequently grants staff Field authority for `closedAt`.        | The timestamp is server-derived inside a named Mutation, but Collection Policy sees it as an ordinary supplied patch Field. The app must grant the human caller authority over the field even though the browser contract never exposes it. Static Collection `values` cannot express close-versus-reopen conditional derivation.                                     | Let a generated Mutation distinguish trusted server-derived assignments from caller input while still running candidate Policy over the final row. The seam must preserve one lock owner and must not become an internal Runtime binder escape hatch.                                                        | About 5–10 lines here; primarily removes an authority-model mismatch rather than large boilerplate.                                                                                             |

Add rows while authoring and integration expose concrete friction. A zero-line
correctness gain is recorded as zero rather than inflated.
