{
	"protocolVersion": 2,
	"ticket": "#295",
	"proof": "BETA-08 executes the Reaction that BETA-06 already accepts. One committed message-published fact creates one causally deduplicated durable run, and a worker records a Policy-current result through the shared PostgreSQL durable kernel: run, physical attempt, opaque lease fence, append-only history, stable effect ledger, durable cancellation, and an audited maintenance surface.",
	"diffBase": "141417bc96dcb56ab6ce88a8521a11dd323fe76e",
	"reviewOutput": "docs/v4/implementation/beta08/REVIEW.json",
	"authorityHeads": {
		"accepted feat/v4 base": "141417bc96dcb56ab6ce88a8521a11dd323fe76e",
		"BETA-06 accepted evidence": "f9879efdfb2921ed747d353b6cb903398e9d67c3",
		"BETA-07 accepted evidence": "dfa461162fdb211382708b9ad2a30cf10b564015",
		"beta one slice": "0d8e2543ff7e9d50bdab7d2b66b62ec4c35d8a6f"
	},
	"authorityDocuments": [
		{
			"name": "ADR-0013 transactional dispatch and Reaction",
			"path": "docs/adr/0013-freeze-transactional-dispatch-and-reaction.md",
			"sha256": "f8b36c9df0af8c8ca6785fb901cf075744dbcfc8d2586a182855805a920850ed"
		},
		{
			"name": "ADR-0016 lifecycle jobs and the shared durable kernel",
			"path": "docs/adr/0016-freeze-lifecycle-jobs-and-shared-durable-kernel.md",
			"sha256": "7b9e5dca74828f576a21f738fbdbc5335560ca7575a26360ca3b6115fe14ee8f"
		},
		{
			"name": "ADR-0021 beta.1 release slicing",
			"path": "docs/adr/0021-slice-the-beta-one-release.md",
			"sha256": "099aa6347559091cac54b7412c5f318623c4a5d49e50c820b6398c55f1d6a799"
		},
		{
			"name": "accepted transactional dispatch and Reaction contract",
			"path": "docs/v4/transactional-dispatch-and-reaction.md",
			"sha256": "208e3e24b1d6bb65cdb04030cea969d6da25f2611d0d3c7b0c42881a747eeaae"
		},
		{
			"name": "accepted lifecycle jobs and shared durable kernel projection",
			"path": "docs/v4/lifecycle-jobs-and-shared-durable-kernel.md",
			"sha256": "fc19ad32fd021aea7af8cb5335071aaa4529e31c04b6af2cb6bea5e02a5255c8"
		},
		{
			"name": "BETA-08 implementation decision record",
			"path": "docs/v4/implementation/beta08/design-context.md",
			"sha256": "b5d8da75d70f04d4ae65c88c02512d398598a5c46dae5bf6a98ef04682a018bf"
		},
		{
			"name": "implementation gates",
			"path": "docs/v4/implementation-gates.md",
			"sha256": "f454048b193c2a851f8fafe7ecc03cb02430d62ccdf2501f342434d91554ae0f"
		}
	],
	"verification": [
		{
			"command": "bun run check:changed -- --test tests/integration/beta08-reaction.test.ts --typecheck @questpie/runtime",
			"result": "PASS"
		},
		{
			"command": "bun run test:postgres -- --scenario beta08 (16 tests across beta08-durable-kernel, beta08-internal-protocol, and beta08-reaction-worker; 0 failures)",
			"result": "PASS"
		},
		{
			"command": "bun run test:postgres on PostgreSQL 16, 17, and 18 (95, 95, and 98 passing; 0 failures; 3 known BETA-02 catalog version skips on 16 and 17)",
			"result": "PASS"
		},
		{
			"command": "bun run bench:micro -- --scenario beta08 (postgresDurable20Ms 435.042 ms against a 2500 ms budget, 168 maximum run result bytes, 3 events per successful run, 57779 public declaration bytes, 21028 TypeScript instantiations)",
			"result": "PASS"
		},
		{
			"command": "bun run test:load -- --scenario beta08 (96 runs against 8 competing workers in 440.106 ms, 96 attempts, 0 superseded leases, 0 duplicate attempts)",
			"result": "PASS"
		},
		{
			"command": "bun run quality:full",
			"result": "PASS"
		},
		{
			"command": "bun run quality:release",
			"result": "PASS"
		},
		{
			"command": "bun node_modules/typescript/bin/tsc --noEmit -p tests/tsconfig.beta08.json (tests/** has no root tsconfig and is not covered by check-types, so this slice pins its own gate over the eight test files it owns)",
			"result": "PASS"
		},
		{
			"command": "git diff --check",
			"result": "PASS"
		}
	],
	"acceptanceCriteria": [
		"questpie.internal.v4 adds the durable run, attempt, lease, append-only history, effect, cancellation, and maintenance-audit schema, upgrades from v3 in one pinned transaction, and verifies its own catalog, guard functions, and triggers.",
		"One committed message-published fact owns exactly one run: BETA-06's origin-unique dispatch plus durable_run_dispatch_unique make a second Reaction for the same fact unrepresentable rather than merely unlikely, and the dispatch, run, first history event, Message, Message Event, and Mutation receipt share one PostgreSQL transaction.",
		"A worker admits at most 64 ready rows, takes each individual claim with FOR UPDATE SKIP LOCKED, writes one attempt plus a lease, and commits before the handler runs; concurrent claims on one ready run produce exactly one attempt.",
		"Every heartbeat, terminal transition, and effect write compares the current attempt and the lease token, so after a takeover the stale holder can neither heartbeat nor publish success, failure, or cancellation. The prescribed red test is falsified against the unfenced kernel and the exact failing assertion is recorded.",
		"Every physical attempt builds one fresh root Execution from the persisted Context input, resolves Context once, and evaluates current Policy; revoking the caller's Membership makes the attempt terminal with RUN_AS_DENIED, and an operator retry re-evaluates current Policy rather than replaying the earlier decision.",
		"The declared retry program is eight attempts with full-jitter exponential backoff clamped by the persisted run horizon; validation, declared Reaction errors, run-as denial, effect conflict, and effect ambiguity are permanent, and exhaustion or permanent failure creates one inspectable dead letter.",
		"One logical effect keeps one stable effect identity across attempts. A reliable lookup that returns a receipt recovers a lost response, a lookup that proves the provider never accepted the request stays retryable, and an absent or failing lookup records an ambiguity that only an audited acknowledgeAmbiguity can settle. No exactly-once effect claim is made.",
		"A worker without matching executable bytes refuses the claim and consumes no attempt, leaving the run ready for a compatible worker.",
		"acknowledgeAmbiguity, cancelRun, and retryRun each elect exactly one winner under concurrency and record every attempt, applied or rejected, in the maintenance audit with its actor and its state transition.",
		"Direct application and worker writes to dispatch, run, attempt, and event state are rejected; run history rejects update, delete, and truncate even inside a kernel transaction; every questpie_internal index is B-tree with no expression or partial index; and no table enables row-level security.",
		"Events carry sequence, timestamp, Resource, dispatch, run, attempt, lease-token digest, causation, correlation, kind, and a safe error code, and carry no raw payload, credential, secret, or stack trace.",
		"The slice adds no generic Job client, no Workflow steps, no schedules, and no external Action authoring, and the generated browser client gains no durable control plane."
	]
}
