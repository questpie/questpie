{
	"protocolVersion": 2,
	"ticket": "ACTION-KERNEL-WIRE-V3-EFFECT-IDENTITY",
	"profile": "questpie.acceptance.v2",
	"manifestPath": "docs/v4/prototypes/action-wire-v3-effect-identity/acceptance-manifest.json",
	"reviewedHead": "eb3744d35fe56dcb78ab2378ee63b0d29639cf13",
	"diffBase": "c68309f3d0997cb3acb224a53d214d4f1516bbf2",
	"packetDigest": "f0febd29626c2f792025f924258e7e609364c7da6cdd455687f3d68682124a6e",
	"primary": {
		"profile": "claude-opus-medium-v1",
		"disposition": "PASS",
		"findings": "VERDICT: PASS\n\n## Blocking findings\n\nNone.\n\n## Verification of the prior `BLOCKED` repairs\n\n**Prior blocker 1 (unbound durable UUID vector) is repaired.** `repository-check.ts` now tree-binds `packages/runtime` between the canonical and dependency worktrees, imports the production `effectIdentity` from `packages/runtime/src/durable/rows.ts`, and asserts three application/run/effect vectors against real production output, that the proof's `deriveDurableEffectIdentity` model equals production for each, and that ordinary Action derivation under `questpie.effect-identity.action.v1` collides with none of them. The manifest pins `packages/runtime/src/durable/rows.ts` and `tests/unit/pb05-postgres-database-durable-effect-reserve.test.ts` with SHA-256, and that test independently sources the `64a789a4-c319-5d2b-ac27-520d9808a941` vector from `application:collaboration` / `018f5f6e-…61a0` / `deliver` through the real reservation path. Criterion 3 was also rewritten to claim exactly what the gate proves. The gate fails closed (`exit 1`) when no matching dependency worktree exists, so it cannot silently skip.\n\n**Prior blocker 2 (head provenance) is materially addressed and no longer blocking.** `authorityHeads` now names `f71c5954` as the replacement combined proof head and retains `fb099307` as the prior blocked reviewed head. The reviewed head `eb3744d3` is a later commit, so the manifest still does not name itself — but no committed manifest can name its own hash, the label as written is accurate rather than false, every packet byte is read from the reviewed commit, and each listed command is re-runnable at `eb3744d3`. I do not treat an unsatisfiable-by-hash attestation as missing evidence for any acceptance criterion.\n\n**Criteria coverage checked at the reviewed head:** effectKey grammar and alias rejection (1), domain-separated derivation with excluded input/`callId` and canonical-identity bounds (2), durable binding (3), no automatic retry/coalescing with stable identity across duplicate admissions (4), closed additive Wire v3 with globally sorted operation/failure arrays, retained v2 field equality, hostile re-signing rejection, and pre-Context/Service/handler `CLIENT_OUTDATED` for v1/v2 Action and selected/request confusion (5), zero-work pre-execution rejection plus eight non-retryable `ACTION_OUTCOME_AMBIGUOUS` transport families with `callId`-only payload (6), exact three-member limits and post-Policy zero-duration ordering (7), known-outcome precedence and post-handler `RESOURCE_LIMIT` including oversized authored `outcomeUnknown` (8), remaining-duration conversion under distinct caller/Runtime clocks (9), recursive framework-owned nondisclosure with authored codec payloads deliberately exempt (10). Scope discipline (12) holds: the diff touches only `docs/v4/prototypes/**` and adds no ADR, public documentation, compiler, Runtime, client, schema, or release artifact.\n\n## Non-blocking observations\n\n- `wire-v3.json` now pins `preExecutionRejection.frameKind: \"failure\"`, while retained `responseKeys.failure` is `[\"callId\",\"error\",\"kind\",\"operation\",\"protocol\"]` and `responseKeys.rejection` is `[\"error\",\"kind\"]`. The same `kind` literal therefore carries two disjoint key sets distinguished only by correlation presence, which is how `createActionHarness` validates frames. This does not change retained v2 bytes, but the implementation slice should either introduce a distinct rejection `kind` literal or pin a `responseKeys` selector for it.\n- `type-contract.ts` improved: the `effectId` and `idempotencyKey` negatives now include `effectKey`, so they discriminate excess properties. `mutationAlias` still fails primarily because required `effectKey` is absent and `callId` is a legitimate optional member; alias rejection is really carried by the exact-request-key check in `admitOperationRequest`.\n- `deriveEffectIdentity` excludes `input` and `callId` by construction (it builds an explicit six-field object), so those equality assertions are true but tautological rather than falsifying.\n- `repository-check.ts` compares committed tree hashes (`git rev-parse HEAD:path`) but imports working-tree files from the dependency root. A dirty dependency worktree could load bytes other than the compared trees; the manifest's pinned `rows.ts` digest mitigates this for a reader, not for the gate.\n- `createActionHarness` still selects outcomes from a `ProviderOutcome` switch, so the ambiguity family proves disposition shape, correlation payload, and direct/network parity rather than real transport behavior. `DECISION.md` states this honestly; the implementation slice owes actual adapter evidence.\n- The stale `Base: 79b56b6e…` header in `docs/v4/prototypes/action-limits/DECISION.md` flagged by the prior record is corrected to `c68309f3`.\n- The manifest would be stronger if it stated explicitly that only manifest metadata changed after the named `f71c5954` proof head, so the recorded `PASS` results are attributable to the reviewed bytes without inference."
	},
	"verdict": "PASS",
	"recordedAt": "2026-08-24T14:35:00.486Z"
}
