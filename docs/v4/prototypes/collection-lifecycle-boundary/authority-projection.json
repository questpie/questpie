{
	"format": "questpie.authority-projection.git-diff-lines.v1",
	"base": "a2ea015d081d2abb95985f1c3db9cbe2a59a3cd6",
	"candidate": "d28df9c2d00ae8c0e73af047ed99e0df05816783",
	"note": "Candidate evidence only. Join lines with LF to reconstruct the exact selected-file diff; do not apply before a verified PASS.",
	"lines": [
		"diff --git a/CONTEXT.md b/CONTEXT.md",
		"index 81b52661b..9c93f2fbd 100644",
		"--- a/CONTEXT.md",
		"+++ b/CONTEXT.md",
		"@@ -427,6 +427,34 @@ Query.",
		" A semantic Operation that owns one PostgreSQL transaction and its atomic",
		" Transactional Dispatch boundary.",
		" ",
		"+### Collection Issue",
		"+",
		"+A payloadless, Collection-owned identity for an expected `validate` or `check`",
		"+invariant failure. It has no public code, status, message, payload, or transport",
		"+meaning. A named Mutation explicitly maps every reachable Collection Issue to",
		"+one of its own declared Operation errors. Unmapped or forged issues roll back",
		"+and sanitize to `INTERNAL`.",
		"+",
		"+Do not use: validation error, database error, Operation error.",
		"+",
		"+### Lifecycle Program",
		"+",
		"+The versioned canonical program compiled from a Collection's `normalize`,",
		"+`validate`, `check`, and `afterWrite` TypeScript syntax. Runtime interprets the",
		"+program and never invokes the authored callbacks as JavaScript. Phase-specific",
		"+grammar makes purity, capability closure, ordering, and artifact integrity",
		"+executable guarantees.",
		"+",
		"+Do not use: hook, callback runtime, lifecycle plugin.",
		"+",
		"+### Operation Error",
		"+",
		"+A named, typed failure declared by one Operation and exposed consistently by",
		"+direct and wire calls. Operation-owned issue mapping may translate a",
		"+payloadless Collection Issue only after the owning transaction rolls back.",
		"+",
		"+Do not use: Collection Issue, PostgreSQL error.",
		"+",
		" ### Operation Result Receipt",
		" ",
		" The transaction-owned record of one committed Mutation call and its exact",
		"diff --git a/SPEC.md b/SPEC.md",
		"index 157423e31..95085ff6d 100644",
		"--- a/SPEC.md",
		"+++ b/SPEC.md",
		"@@ -620,6 +620,18 @@ ADR-0022 freezes the generated authoring ergonomics and Operation projection.",
		" ADR-0023 supersedes ADR-0014's incomplete post-commit Mutation outcome with",
		" Operation Wire v2 while retaining v1 Query compatibility.",
		" ",
		"+ADR-0030 and ADR-0031 deepen the existing generated Collection write kernel.",
		"+Field provenance derives exact caller input; a disjoint trusted `values` lane",
		"+bypasses only caller Field authority; the complete candidate still passes",
		"+validation and candidate Policy. Exactly four Collection lifecycle phases are",
		"+authored as ordinary TypeScript syntax compiled into one canonical lifecycle",
		"+program: capability-free `normalize` and `validate`, Policy-aware transactional",
		"+`check`, and pre-commit bounded `afterWrite`. Collections raise payloadless",
		"+issues; each named Mutation explicitly maps every transitively reachable issue",
		"+to its own declared error. `ctx.now` is the transaction-stable clock, and",
		"+`onUpdate: \"now\"` is database-owned. No Collection becomes public merely by",
		"+existing, and no second CRUD or lifecycle kernel exists.",
		"+",
		" ## 17. Current delivery sequence and historical grilling order",
		" ",
		" ADR-0027 makes this the current sequence:",
		"diff --git a/apps/docs/content/docs/v4/queries-and-mutations.mdx b/apps/docs/content/docs/v4/queries-and-mutations.mdx",
		"index fa4394f23..373fd82b5 100644",
		"--- a/apps/docs/content/docs/v4/queries-and-mutations.mdx",
		"+++ b/apps/docs/content/docs/v4/queries-and-mutations.mdx",
		"@@ -1,590 +1,314 @@",
		" ---",
		" title: Build Queries and Mutations",
		"-description: Define exact semantic reads, transactional writes, and ordinary Collection operations.",
		"+description: Define exact reads, transactional writes, Collection lifecycle programs, and public errors.",
		" kind: guide",
		" ---",
		" ",
		" QUESTPIE compiles each Query and Mutation into one statically bound Operation.",
		"-A Query reads from one consistent snapshot. A Mutation owns one PostgreSQL",
		"-transaction. Both use the same generated types, Policy, errors, limits, and",
		"-observation path for direct and network calls.",
		"-",
		"-The examples continue with the complete collaboration fixture from",
		"-[Resolve Context and authorize with Policy](./context-and-policy). Its Message",
		"-Collection supplies the Fields selected and written below.",
		"-",
		"-Define the reusable structural plan for the paged part of the view:",
		"-",
		"-```ts title=\"src/features/message-data.ts\"",
		"-import type { AppData } from \"#questpie/app\";",
		"-import { dataQuery, query } from \"questpie\";",
		"-",
		"-export const channelMessagePage = dataQuery<",
		"-\tAppData[\"collections\"][\"messages\"]",
		"->()({",
		"-\tfrom: \"messages\",",
		"-\tparameters: {",
		"-\t\tchannelId: query.parameter.uuid({ nullable: false }),",
		"-\t\tfirst: query.parameter.integer({",
		"-\t\t\tnullable: false,",
		"-\t\t\tminimum: 1,",
		"-\t\t\tmaximum: 100,",
		"-\t\t}),",
		"-\t\tafter: query.parameter.cursor({ nullable: true }),",
		"-\t},",
		"-\tselect: ({ fields }) => ({",
		"-\t\tid: fields.id,",
		"-\t\tauthorId: fields.authorId,",
		"-\t\ttitle: fields.title,",
		"-\t\tbody: fields.body,",
		"-\t\tcreatedAt: fields.createdAt,",
		"-\t}),",
		"-\twhere: ({ fields, parameters }) =>",
		"-\t\tfields.channelId.equal(parameters.channelId),",
		"-\torderBy: ({ fields }) => [",
		"-\t\tfields.createdAt.ascending({ nulls: \"last\" }),",
		"-\t\tfields.id.ascending({ nulls: \"last\" }),",
		"-\t],",
		"-\tpage: ({ parameters }) =>",
		"-\t\tquery.forwardCursor({",
		"-\t\t\tfirst: parameters.first,",
		"-\t\t\tafter: parameters.after,",
		"-\t\t}),",
		"-});",
		"-```",
		"+A Query reads one consistent snapshot. A Mutation owns one PostgreSQL",
		"+transaction. Direct calls and the generated network client enter the same",
		"+Operation engine and therefore share Policy, declared errors, cancellation,",
		"+limits, and transaction outcomes.",
		" ",
		"-## Define a read-only Query",
		"+## Define a Query",
		" ",
		"-Import the application-specialized factory from the generated App Contract:",
		"+Use the application-specialized factory from `#questpie/app`. Its generated",
		"+Context exposes Policy-aware reads but no write, dispatch, raw SQL, transaction,",
		"+or external-effect capability.",
		" ",
		"-```ts title=\"src/features/channel-overview.ts\"",
		"+```ts",
		" import { defineQuery } from \"#questpie/app\";",
		"-import { operation, policy } from \"questpie\";",
		"-import { channelMessagePage } from \"./message-data\";",
		"+import { codec, operation, policy } from \"questpie\";",
		" ",
		"-export const channelOverview = defineQuery({",
		"-\tname: \"channels.overview\",",
		"-\tinput: operation.input(channelMessagePage),",
		"+export const ticketDetail = defineQuery({",
		"+\tname: \"tickets.detail\",",
		"+\tinput: codec.object({ id: codec.uuid() }),",
		" \tpolicy: policy.authenticated(),",
		" \terrors: {",
		" \t\tunavailable: operation.error({",
		"-\t\t\tcode: \"CHANNEL_UNAVAILABLE\",",
		"+\t\t\tcode: \"TICKET_UNAVAILABLE\",",
		" \t\t\tstatus: 404,",
		" \t\t}),",
		" \t},",
		" \thandler: async ({ input, ctx, errors }) => {",
		"-\t\tconst company = await ctx.data.companies.get({",
		"-\t\t\tkey: { id: ctx.tenant.id },",
		"-\t\t\tselect: { id: true, name: true },",
		"-\t\t});",
		"-\t\tconst membership = await ctx.data.memberships.get({",
		"-\t\t\tkey: {",
		"-\t\t\t\tcompanyId: ctx.tenant.id,",
		"-\t\t\t\tprincipalId: ctx.principal.id,",
		"-\t\t\t\tscopeKey: \"company\",",
		"-\t\t\t},",
		"-\t\t\tselect: { role: true },",
		"+\t\tconst ticket = await ctx.data.tickets.get({",
		"+\t\t\tkey: { id: input.id },",
		"+\t\t\tselect: { id: true, reference: true, summary: true, status: true },",
		" \t\t});",
		"-\t\tconst channel = await ctx.data.channels.get({",
		"-\t\t\tkey: { id: input.channelId },",
		"-\t\t\tselect: { id: true, companyId: true, name: true },",
		"-\t\t});",
		"-",
		"-\t\tif (",
		"-\t\t\tcompany === null ||",
		"-\t\t\tmembership === null ||",
		"-\t\t\tchannel === null ||",
		"-\t\t\tchannel.companyId !== company.id",
		"-\t\t) {",
		"-\t\t\tthrow errors.unavailable();",
		"-\t\t}",
		"-",
		"-\t\tconst messages = await ctx.data.run(channelMessagePage, input);",
		"-\t\treturn {",
		"-\t\t\tcompany,",
		"-\t\t\tmembership,",
		"-\t\t\tchannel: { id: channel.id, name: channel.name },",
		"-\t\t\tmessages,",
		"-\t\t};",
		"+\t\tif (ticket === null) throw errors.unavailable();",
		"+\t\treturn ticket;",
		" \t},",
		" \tnetwork: true,",
		" });",
		" ```",
		" ",
		"-The input reuses the exact parameters of the structural data plan. The handler",
		"-return determines the output codec when the value uses supported scalars,",
		"-exact objects, nullable values, and arrays.",
		"-",
		"-The Query Context contains Policy-aware reads and",
		"-`ctx.data.run(plan, input)`, alongside immutable execution facts such as the",
		"-Principal, Tenant, signal, and deadline. It cannot write, dispatch, open a raw",
		"-transaction, run raw SQL, bypass Policy, or select System Authority. All",
		"-handler reads share one bounded `REPEATABLE READ READ ONLY` snapshot.",
		"+Missing and Policy-invisible rows both return `null`. Application code cannot",
		"+use a read to distinguish them. `network: true` publishes this named Operation;",
		"+defining a Collection never publishes CRUD.",
		" ",
		"-For a named Query or Mutation, `policy: policy.authenticated()` is its",
		"-Operation-level admission rule. It runs before the handler. A Collection",
		"-Operation Set instead receives the Collection-bound relational Policy, such as",
		"-`messagePolicy`, so every generated member uses the Collection's admission,",
		"-row, candidate, and Field rules.",
		"+Use an explicit output codec when a contract must remain pinned or is recursive.",
		"+Otherwise the compiler may infer a supported closed result and materialize its",
		"+runtime codec from the same generated App Contract.",
		" ",
		"-`network: true` exposes the Operation through the generated network client. If",
		"-it is absent, the Operation remains available only from the generated server",
		"-contract.",
		"+## Derive exact caller input",
		" ",
		"-Use an explicit output pin when the public result must remain stable or is",
		"-recursive:",
		"+Field provenance derives caller codecs without granting network exposure:",
		" ",
		"-```ts title=\"src/features/thread-preview.ts\"",
		"-import { defineQuery } from \"#questpie/app\";",
		"-import { codec, policy } from \"questpie\";",
		"-import type { Codec } from \"questpie\";",
		"-",
		"-interface ThreadNode {",
		"-\treadonly id: string;",
		"-\treadonly replies: ReadonlyArray<ThreadNode>;",
		"-}",
		"-",
		"-const threadNode: Codec<ThreadNode> = codec.lazy(() =>",
		"-\tcodec.object({",
		"-\t\tid: codec.uuid(),",
		"-\t\treplies: codec.array(threadNode),",
		"-\t}),",
		"-);",
		"+```ts",
		"+export const createTicketInput = tickets.createInput().pick({",
		"+\tteamId: true,",
		"+\treference: true,",
		"+\tsummary: true,",
		"+\tdescription: true,",
		"+});",
		"+```",
		" ",
		"-export const threadPreview = defineQuery({",
		"-\tname: \"messages.threadPreview\",",
		"-\tinput: codec.object({ id: codec.uuid() }),",
		"-\toutput: threadNode,",
		"-\tpolicy: policy.authenticated(),",
		"-\terrors: {},",
		"-\thandler: ({ input }) => ({ id: input.id, replies: [] }),",
		"-\tnetwork: true,",
		"+Default Fields are caller-writable on create and update. `immutable: true`",
		"+Fields are caller-writable only on create. `server: true` Fields are absent from",
		"+both caller helpers and are available only to the trusted `values` lane.",
		"+`onUpdate: \"now\"` Fields are database-owned and are available to neither lane.",
		"+",
		"+`createInput()` applies create requiredness after provenance. `updateInput()` is",
		"+sparse. `.pick({ field: true })` and `.omit({ field: true })` preserve exact",
		"+Field codecs and reject unknown Fields. A derived helper intentionally evolves",
		"+when its Collection changes; pin a smaller key set when an Operation must stay",
		"+stable.",
		"+",
		"+## Give a Collection a fixed lifecycle",
		"+",
		"+Collections may own exactly four lifecycle phases and payloadless issues:",
		"+",
		"+```ts",
		"+import { collection, defineCollection, field } from \"questpie\";",
		"+",
		"+export const tickets = defineCollection({",
		"+\tname: \"tickets\",",
		"+\tfields: {",
		"+\t\tid: field.uuid({ default: \"randomUuid\", server: true, immutable: true }),",
		"+\t\treference: field.text({ minLength: 1, maxLength: 32, immutable: true }),",
		"+\t\tsummary: field.text({ minLength: 1, maxLength: 240 }),",
		"+\t\tstatus: field.text({ default: \"open\", server: true }),",
		"+\t\tclosedAt: field.timestamp({ nullable: true, server: true }),",
		"+\t\tupdatedAt: field.timestamp({ default: \"now\", onUpdate: \"now\" }),",
		"+\t},",
		"+\tconstraints: {",
		"+\t\t/* primary key and unique constraints */",
		"+\t},",
		"+\tissues: {",
		"+\t\tclosedWithoutTime: collection.issue(),",
		"+\t\tteamUnavailable: collection.issue(),",
		"+\t},",
		"+\tlifecycle: {",
		"+\t\tnormalize: ({ input }) => ({",
		"+\t\t\t...input,",
		"+\t\t\treference: input.reference?.trim().toUpperCase(),",
		"+\t\t\tsummary: input.summary?.trim(),",
		"+\t\t}),",
		"+\t\tvalidate: ({ candidate, issues }) => {",
		"+\t\t\tif (candidate.status === \"closed\" && candidate.closedAt === null)",
		"+\t\t\t\tthrow issues.closedWithoutTime();",
		"+\t\t},",
		"+\t},",
		" });",
		" ```",
		" ",
		"-The pin applies the same validation and encoding contract described in",
		"-[Define executable application behavior](./executable-definitions). Recursive",
		"-output requires this explicit contract.",
		"+A Collection issue says which internal invariant failed. It carries no public",
		"+code, status, message, payload, protected value, or transport behavior. The",
		"+Operation decides what its caller may learn.",
		" ",
		"-A supported Query also exposes `.watch` on the same generated method. See",
		"-[Watch Queries in realtime](./realtime) for complete results, observed reads,",
		"-fresh Policy, resume/reset, and Change Ledger recovery.",
		"+Lifecycle callbacks use ordinary TypeScript syntax, but they are compiled",
		"+definitions, not arbitrary JavaScript callbacks. The compiler lowers supported",
		"+syntax to one canonical Lifecycle Program and Runtime interprets that program.",
		"+Authored lifecycle JavaScript is never invoked.",
		" ",
		"-## Derive caller input from a Collection",
		"+`normalize` and `validate` allow immutable locals, branches, exact object",
		"+construction, static Field access, deterministic operators, and documented",
		"+locale-free scalar methods. They reject imports and captures, ambient clocks,",
		"+randomness, `fetch`, dynamic property access, constructors, mutation, arbitrary",
		"+calls, async work, and unsupported built-ins with an Origin and a supported",
		"+rewrite.",
		" ",
		"-A Collection can derive exact caller-input codecs from its Field provenance.",
		"-The Barbershop schedules an appointment with a narrowed create input:",
		"+`normalize` runs separately over the already authorized caller and trusted",
		"+lanes. It may replace supplied values but cannot add, remove, move, or overlap",
		"+Field paths. `validate` sees the complete candidate and optional current row,",
		"+uses the transaction-stable `now`, and can throw a declared Collection issue.",
		" ",
		"-```ts title=\"src/features/schedule-appointment.ts\"",
		"-import { defineMutation } from \"#questpie/app\";",
		"-import type { CodecValue } from \"questpie\";",
		"-import { policy } from \"questpie\";",
		"+## Map a Collection issue to a public error",
		" ",
		"-import { appointments } from \"../data/appointments\";",
		"+A named Mutation owns public errors and maps every reachable Collection issue",
		"+explicitly:",
		" ",
		"-export const scheduleAppointmentInput = appointments.createInput().pick({",
		"-\tcustomerName: true,",
		"-\tstartsAt: true,",
		"-\tendsAt: true,",
		"-});",
		"+```ts",
		"+import { defineMutation } from \"#questpie/app\";",
		"+import { operation, policy } from \"questpie\";",
		" ",
		"-type ScheduleAppointmentInput = CodecValue<typeof scheduleAppointmentInput>;",
		"-// Readonly<{ customerName: string; startsAt: Date; endsAt: Date }>",
		"+const invalidTicket = operation.error({",
		"+\tcode: \"INVALID_TICKET\",",
		"+\tstatus: 422,",
		"+});",
		" ",
		"-export const scheduleAppointment = defineMutation({",
		"-\tname: \"appointments.schedule\",",
		"-\tinput: scheduleAppointmentInput,",
		"+export const createTicket = defineMutation({",
		"+\tname: \"ticket.create\",",
		"+\tinput: tickets.createInput(),",
		" \tpolicy: policy.authenticated(),",
		"-\terrors: {},",
		"+\terrors: { invalidTicket },",
		"+\tissueMappings: {",
		"+\t\ttickets: {",
		"+\t\t\tclosedWithoutTime: \"invalidTicket\",",
		"+\t\t\tteamUnavailable: \"invalidTicket\",",
		"+\t\t},",
		"+\t},",
		" \thandler: ({ input, ctx }) =>",
		"-\t\tctx.data.appointments.create({",
		"+\t\tctx.data.tickets.create({",
		" \t\t\tinput,",
		"-\t\t\tvalues: {",
		"-\t\t\t\ttenantId: ctx.tenant.id,",
		"-\t\t\t\tcreatedAt: ctx.operationTime,",
		"-\t\t\t\tupdatedAt: ctx.operationTime,",
		"-\t\t\t},",
		"+\t\t\tvalues: { organizationId: ctx.tenant.id },",
		"+\t\t\tselect: true,",
		" \t\t}),",
		" \tnetwork: true,",
		" });",
		" ```",
		" ",
		"-The generated client infers both the call and its result:",
		"-",
		"-```ts title=\"web/schedule-appointment.ts\"",
		"-import type { GeneratedClient } from \"#questpie/client\";",
		"-",
		"-export async function scheduleAppointment(client: GeneratedClient) {",
		"-\tconst appointment = await client.mutations[\"appointments.schedule\"]({",
		"-\t\tcustomerName: \"Marek Novak\",",
		"-\t\tstartsAt: new Date(\"2026-09-01T09:00:00.000Z\"),",
		"-\t\tendsAt: new Date(\"2026-09-01T09:30:00.000Z\"),",
		"-\t});",
		"-",
		"-\tappointment.status satisfies string;",
		"-\treturn appointment;",
		"-}",
		"-```",
		"-",
		"-`createInput()` includes caller-writable create Fields. `updateInput()` excludes",
		"-server-owned and immutable Fields, and every remaining member is optional.",
		"-`.pick({ field: true })` and `.omit({ field: true })` preserve each Field codec",
		"-and reject an unknown Field in TypeScript and during compilation. The helper is",
		"-a Codec value. It does not define or publish an Operation.",
		"-",
		"-A derived surface intentionally follows its Collection. Adding a new Field",
		"-changes `createInput()` or `updateInput()` when that Field's provenance belongs",
		"-to the helper, and the generated App Contract makes the change visible to",
		"-TypeScript. Use `.pick()` or `.omit()` when an Operation must expose a smaller,",
		"-pinned **key set**. A selected Field's own contract still follows the Field: a",
		"-bound, nullability, scalar, or nested-codec change intentionally changes the",
		"-derived Operation contract and its generated types.",
		"-",
		"-Required, nullable, defaulted, immutable, and server Fields remain distinct in",
		"-the inferred type. On create, a non-nullable Field without a default is",
		"-required; a nullable Field or a Field with a schema default is optional. A",
		"-nullable timestamp, when present, is `Date | null`. An immutable caller Field",
		"-appears on create and disappears from update. A server Field appears in neither",
		"-caller helper. Embedded objects and arrays retain their nested codecs and",
		"-bounds instead of widening to `unknown`.",
		"-",
		"-## Add ordinary Collection operations",
		"-",
		"-Define the common Collection surface once:",
		"-",
		"-```ts title=\"src/features/message-operations.ts\"",
		"-import { defineCollectionOperations, mutation, operation } from \"questpie\";",
		"-import { messages } from \"../data/collaboration\";",
		"-import { channelMessagePage } from \"./message-data\";",
		"-import { messagePolicy } from \"./messages-policy\";",
		"-",
		"-export const messageOperations = defineCollectionOperations(messages, {",
		"-\tname: \"messages\",",
		"-\tpolicy: messagePolicy,",
		"-\tnetwork: true,",
		"-",
		"-\tlist: { data: channelMessagePage },",
		"-\tget: {",
		"-\t\tselect: {",
		"-\t\t\tid: true,",
		"-\t\t\ttitle: true,",
		"-\t\t\tbody: true,",
		"-\t\t\tcreatedAt: true,",
		"-\t\t\tupdatedAt: true,",
		"-\t\t},",
		"-\t},",
		"-\tcreate: {",
		"-\t\tinput: [\"channelId\", \"title\", \"body\"],",
		"-\t\tnormalize: ({ input }) => ({",
		"-\t\t\ttitle: operation.text.trim(input.title),",
		"-\t\t}),",
		"-\t\tvalues: ({ principal, tenant, operationTime }) => ({",
		"-\t\t\tcompanyId: mutation.overwrite(tenant.id),",
		"-\t\t\tauthorId: mutation.overwrite(principal.id),",
		"-\t\t\tcreatedAt: mutation.overwrite(operationTime),",
		"-\t\t\tupdatedAt: mutation.overwrite(operationTime),",
		"-\t\t}),",
		"-\t\tselect: {",
		"-\t\t\tid: true,",
		"-\t\t\tchannelId: true,",
		"-\t\t\tauthorId: true,",
		"-\t\t\ttitle: true,",
		"-\t\t\tbody: true,",
		"-\t\t\tcreatedAt: true,",
		"-\t\t\tupdatedAt: true,",
		"-\t\t},",
		"-\t},",
		"-\tupdate: {",
		"-\t\tinput: [\"title\", \"body\"],",
		"-\t\tnormalize: ({ input }) => ({",
		"-\t\t\ttitle: operation.text.trimIfPresent(input.title),",
		"-\t\t}),",
		"-\t\tvalues: ({ operationTime }) => ({",
		"-\t\t\tupdatedAt: mutation.overwrite(operationTime),",
		"-\t\t}),",
		"-\t\tselect: { id: true, title: true, body: true, updatedAt: true },",
		"-\t},",
		"-\tdelete: { select: { id: true } },",
		"-});",
		"-```",
		"-",
		"-The compiler lowers each selected member to an ordinary Resource:",
		"-",
		"-| Member   | Resource                   | Consistency boundary  |",
		"-| -------- | -------------------------- | --------------------- |",
		"-| `list`   | Query `messages.list`      | read snapshot         |",
		"-| `get`    | Query `messages.get`       | read snapshot         |",
		"-| `create` | Mutation `messages.create` | one write transaction |",
		"-| `update` | Mutation `messages.update` | one write transaction |",
		"-| `delete` | Mutation `messages.delete` | one write transaction |",
		"-",
		"-The generated names in the table use the ordinary Resource contract described",
		"-in [Define executable application behavior](./executable-definitions).",
		"-",
		"-The set is explicit Operation authoring and exposure shorthand. It is separate",
		"-from the generated Collection create/update kernel used by named Mutations.",
		"-Neither defining a Collection nor calling an input helper publishes CRUD.",
		"-",
		"-The generated client accepts only the declared caller Fields:",
		"-",
		"-```ts title=\"web/edit-message.ts\"",
		"-import type { GeneratedClient } from \"#questpie/client\";",
		"-",
		"-export async function editMessage(",
		"-\tclient: GeneratedClient,",
		"-\tcompanyId: string,",
		"-\tchannelId: string,",
		"-) {",
		"-\tconst company = client.withContext({ companyId });",
		"-\tconst callId = crypto.randomUUID();",
		"-",
		"-\tconst created = await company.collections.messages.create(",
		"-\t\t{",
		"-\t\t\tinput: {",
		"-\t\t\t\tchannelId,",
		"-\t\t\t\ttitle: \"Ready for review\",",
		"-\t\t\t\tbody: \"Please check this message.\",",
		"-\t\t\t},",
		"-\t\t},",
		"-\t\t{ callId },",
		"-\t);",
		"-",
		"-\tconst updated = await company.collections.messages.update({",
		"-\t\tkey: { id: created.id },",
		"-\t\tpatch: { title: \"Approved\" },",
		"-\t});",
		"-",
		"-\tif (updated !== null) {",
		"-\t\tawait company.collections.messages.delete({",
		"-\t\t\tkey: { id: updated.id },",
		"+The mapping refers to a key in this Mutation's `errors` map. The target error",
		"+must be payloadless. Another Mutation may map the same Collection issue to a",
		"+different public error. A Collection never borrows the calling Mutation's error",
		"+factories.",
		"+",
		"+The compiler computes transitive issue reachability. If `comments.afterWrite`",
		"+writes `tickets`, the outer Mutation must map issues reachable through that",
		"+nested call. A missing mapping is a compile error that shows the call path. The",
		"+generated Mutation Context also withholds an issue-bearing kernel method when",
		"+its mapping is incomplete.",
		"+",
		"+The first issue thrown in program order wins and immediately dooms the",
		"+transaction. Catching it in application code cannot permit commit. After",
		"+rollback, the shared Operation engine applies the mapping. An unmapped,",
		"+malformed, or forged issue becomes a sanitized `INTERNAL` failure. Direct and",
		"+network callers therefore see the same declared error and never receive a",
		"+Collection identity, candidate row, protected Field, Policy evidence,",
		"+PostgreSQL detail, or stack.",
		"+",
		"+Unsupported syntax, a captured value, or a phase capability mismatch reports",
		"+`QP-COMPOSE-026` at the lifecycle Origin. An invalid or incomplete issue map",
		"+reports `QP-COMPOSE-027` with the complete nested Collection-call path. Supplying",
		"+an `onUpdate` Field reports `QP-DATA-023 databaseOwnedField`; exceeding the",
		"+compiled lifecycle re-entry bound reports `QP-DATA-024",
		"+lifecycleRecursionExceeded` and rolls back.",
		"+",
		"+## Use `check` for database-backed integrity",
		"+",
		"+Policy authorizes. `validate` checks one candidate without capabilities.",
		"+`check` verifies an invariant that needs current database facts:",
		"+",
		"+```ts",
		"+lifecycle: {",
		"+\tcheck: async ({ candidate, ctx, issues }) => {",
		"+\t\tconst team = await ctx.data.teams.get({",
		"+\t\t\tkey: { id: candidate.teamId },",
		"+\t\t\tselect: { id: true },",
		" \t\t});",
		"-\t}",
		"+\t\tif (team === null) throw issues.teamUnavailable();",
		"+\t},",
		" }",
		" ```",
		" ",
		"-The network call option accepts a stable bounded text `callId`; it is not",
		"-UUID-only. A supplied value must be valid Unicode, already NFC, contain 1–256",
		"-Unicode scalar values, exclude U+0000, and occupy at most 1,024 UTF-8 bytes.",
		"-The Runtime rejects rather than normalizes it. The generated client mints a UUID",
		"-when the caller omits the value. This example deliberately supplies that UUID",
		"-because the application also chooses to use it as a UUID Message key.",
		"-",
		"-The caller cannot supply `companyId`, `authorId`, `createdAt`, or `updatedAt`.",
		"-This Collection Operation Set's closed value programs own them. Those programs",
		"-cannot read data, call a Service, perform I/O, dispatch work, obtain a clock, or",
		"-target an undeclared Field.",
		"-",
		"-## Write several Collections in one Mutation",
		"-",
		"-Use a named Mutation for a cross-Collection invariant or application error:",
		"-",
		"-```ts title=\"src/features/submit-message.ts\"",
		"-import { defineMutation } from \"#questpie/app\";",
		"-import { operation, policy } from \"questpie\";",
		"-import { messages } from \"../data/collaboration\";",
		"-",
		"-export const submitMessage = defineMutation({",
		"-\tname: \"messages.submit\",",
		"-\tinput: messages.createInput().pick({",
		"-\t\tchannelId: true,",
		"-\t\ttitle: true,",
		"-\t\tbody: true,",
		"-\t}),",
		"-\tpolicy: policy.authenticated(),",
		"-\terrors: {",
		"-\t\tchannelUnavailable: operation.error({",
		"-\t\t\tcode: \"CHANNEL_UNAVAILABLE\",",
		"-\t\t\tstatus: 404,",
		"-\t\t}),",
		"-\t},",
		"-\thandler: async ({ input, ctx, errors }) => {",
		"-\t\tctx.signal.throwIfAborted();",
		"+Candidate Policy runs before `check`; an unauthorized candidate cannot use it",
		"+as an existence oracle. Every `check` read is Policy-aware and selection-",
		"+authorized, remains inside the Mutation transaction, and counts against its",
		"+statement, row, dependency, duration, and cancellation budgets. Missing and",
		"+invisible are still indistinguishable.",
		" ",
		"-\t\tconst channel = await ctx.data.channels.get({",
		"-\t\t\tkey: { id: input.channelId },",
		"-\t\t\tselect: { id: true, companyId: true },",
		"-\t\t});",
		"-\t\tif (channel === null || channel.companyId !== ctx.tenant.id) {",
		"-\t\t\tthrow errors.channelUnavailable();",
		"-\t\t}",
		"-",
		"-\t\tconst message = await ctx.data.messages.create({",
		"-\t\t\tinput: {",
		"-\t\t\t\tchannelId: channel.id,",
		"-\t\t\t\ttitle: input.title.trim(),",
		"-\t\t\t\tbody: input.body,",
		"-\t\t\t},",
		"-\t\t\tvalues: {",
		"-\t\t\t\tcompanyId: ctx.tenant.id,",
		"-\t\t\t\tauthorId: ctx.principal.id,",
		"-\t\t\t\tcreatedAt: ctx.operationTime,",
		"-\t\t\t\tupdatedAt: ctx.operationTime,",
		"-\t\t\t},",
		"-\t\t\tselect: {",
		"-\t\t\t\tid: true,",
		"-\t\t\t\tchannelId: true,",
		"-\t\t\t\tauthorId: true,",
		"-\t\t\t\ttitle: true,",
		"-\t\t\t\tbody: true,",
		"-\t\t\t\tcreatedAt: true,",
		"-\t\t\t\tupdatedAt: true,",
		"-\t\t\t},",
		"-\t\t});",
		"+## Use `afterWrite` only for bounded transactional work",
		" ",
		"-\t\tawait ctx.data.channels.update({",
		"-\t\t\tkey: { id: channel.id },",
		"-\t\t\tvalues: {",
		"-\t\t\t\tlastMessageAt: ctx.operationTime,",
		"-\t\t\t\tupdatedAt: ctx.operationTime,",
		"-\t\t\t},",
		"-\t\t\tselect: { id: true },",
		"-\t\t});",
		"+`afterWrite` runs after PostgreSQL constraints and database-owned values have",
		"+produced the written row, but before commit:",
		" ",
		"-\t\tawait ctx.data.messageEvents.create({",
		"-\t\t\tinput: {},",
		"+```ts",
		"+lifecycle: {",
		"+\tafterWrite: async ({ row, previous, ctx }) => {",
		"+\t\tawait ctx.data.ticketEvents.create({",
		" \t\t\tvalues: {",
		"-\t\t\t\tid: `event:${ctx.callId}`,",
		"-\t\t\t\tmessageId: message.id,",
		"-\t\t\t\tkind: \"submitted\",",
		"-\t\t\t\toccurredAt: ctx.operationTime,",
		"+\t\t\t\tticketId: row.id,",
		"+\t\t\t\tkind: previous ? \"updated\" : \"created\",",
		"+\t\t\t\toccurredAt: ctx.now,",
		" \t\t\t},",
		" \t\t\tselect: { id: true },",
		" \t\t});",
		"-",
		"-\t\tawait ctx.dispatch.messageSubmitted({",
		"-\t\t\tmessageId: message.id,",
		"-\t\t\tcompanyId: ctx.tenant.id,",
		"+\t\tawait ctx.jobs.ticket.reindex.accept({",
		"+\t\t\tinput: { ticketId: row.id },",
		"+\t\t\tidempotencyKey: `ticket:${row.id}:${ctx.callId}`,",
		" \t\t});",
		"-",
		"-\t\treturn message;",
		" \t},",
		"-\tnetwork: true,",
		"-});",
		"+}",
		" ```",
		" ",
		"-The Mutation owns one transaction. The Message, Channel update, Message Event,",
		"-Change Ledger fact, result receipt, and durable dispatch intent commit together.",
		"-Caller data enters the Collection kernel through `input` or `patch`. Values",
		"-chosen by the named Mutation enter through the separate trusted `values` lane.",
		"-Both lanes are exact objects, and one Field cannot appear in both. QUESTPIE",
		"-rejects an unknown trusted Field or an overlap before it runs SQL.",
		"-",
		"-The separate objects preserve who supplied each value. Caller Field Policy can",
		"-therefore authorize only caller data, while server-owned values cannot be",
		"-smuggled through the caller lane. Rejecting overlap also avoids an implicit",
		"-last-writer-wins rule.",
		"-",
		"-The Channel call is a values-only update. An empty `patch` with non-empty",
		"-`values` is real work. QUESTPIE rejects an update when both `patch` and `values`",
		"-are empty. Update starts from the locked current row and never reapplies create",
		"-defaults.",
		"-",
		"-Trusted values can use Mutation input, a row returned by a Policy-aware locking",
		"-read, transaction-scoped Policy-aware Collection reads, `ctx.operationTime`,",
		"-`ctx.callId`, and ordinary TypeScript expressions over those values. When a",
		"-value depends on current state, read it through the locking Collection kernel in",
		"-the same transaction. Runtime does not inspect arbitrary TypeScript and does not",
		"-treat a stale handler value as lock-owned evidence.",
		"-",
		"-`operationTime` is stable for that transaction. `createdAt` and `updatedAt`",
		"-remain ordinary Fields and change only through explicit assignments.",
		"-",
		"-Use `ctx.operationTime` when several writes describe the same business change.",
		"-PostgreSQL fixes it once with `transaction_timestamp()` when it creates the",
		"-Mutation owner, and the Runtime carries that trusted fact through the Mutation.",
		"-A Message, its audit row, and its Channel update therefore receive one instant",
		"-even when locks or SQL statements take different amounts of time. It is also",
		"-independent of whichever application process happens to execute the call.",
		"-",
		"-An identical `callId` replay returns the stored result receipt. It does not run",
		"-the handler again or choose a new `operationTime`, so redelivery cannot change",
		"-the already committed business instant.",
		"-",
		"-`new Date()` samples the process clock every time it runs. Two assignments in",
		"-one Mutation can therefore disagree, and application code can accidentally mix",
		"-wall-clock reads taken before and after a lock wait. It also makes the source of",
		"-the value less obvious in tests and review. `operationTime` is an explicit",
		"-trusted execution fact, not caller input and not Policy evidence.",
		"-",
		"-Database-owned `onUpdate` behavior is not part of this surface. Timestamp writes",
		"-remain explicit and transaction-stable.",
		"-",
		"-The generated Mutation Context also contains the stable `callId`, the",
		"-`transactionId`, `ctx.signal`, and the immutable deadline. It exposes no raw",
		"-transaction handle, Policy bypass, external-effect bag, or ordinary-call System",
		"-elevation.",
		"-",
		"-`ctx.dispatch` contains one typed member for each dispatchable Reaction in the",
		"-Current App Contract. The call above targets the app's `messageSubmitted`",
		"-Reaction Definition. This Mutation only accepts its typed intent; it does not",
		"-run the Reaction or any external work in the transaction. See",
		"-[Run durable Jobs and Reactions](./durable-reactions) for attempts, leases,",
		"-retry, current-Policy run-as, external effects, and retention.",
		"-",
		"-## Follow the fixed lifecycle",
		"-",
		"-QUESTPIE processes a Mutation in this order:",
		"-",
		"-1. Decode the exact Operation input and reject unknown keys.",
		"-2. Enforce Operation admission.",
		"-3. Open the transaction and freeze `operationTime`.",
		"-4. Decode the Collection caller `input` or `patch` and trusted `values`.",
		"-5. Reject unknown Collection keys and any Field present in both write lanes.",
		"-6. Apply row scope and acquire the required lock.",
		"-7. Authorize only the Field paths the caller supplied.",
		"-8. Apply closed Field scalar normalization separately to both lanes.",
		"-9. For create, apply absent schema defaults and nullable SQL `NULL` values. For",
		"-   update, start from the locked current row and do not reapply create defaults.",
		"-10. Overlay normalized trusted `values`.",
		"-11. Reject an incomplete required candidate.",
		"-12. Validate every Field in the complete candidate against its framework Codec.",
		"-13. Enforce candidate Policy over the complete merged row.",
		"-14. Enforce PostgreSQL Constraints.",
		"-15. Select and authorize the output.",
		"-16. Validate the output codec.",
		"-17. Commit once.",
		"-18. Encode the result.",
		"-",
		"-The Policy phases use the rules in",
		"-[Resolve Context and authorize with Policy](./context-and-policy). PostgreSQL",
		"-Constraint details never enter the public error.",
		"-",
		"-Each declared error keeps its exact literal code and payload type. An unknown",
		"-thrown value becomes one sanitized `INTERNAL` error without a stack trace or",
		"-database detail.",
		"-",
		"-This candidate validation is framework Field and Codec validation, not an",
		"-authored lifecycle callback. There is no general `before*` or `after*` hook",
		"-catalogue. Use closed Field scalar normalization for the accepted Field codec,",
		"-trusted `values` for server-owned Fields, a named Mutation for cross-Collection",
		"-work and transactional audit, and typed dispatch for durable follow-up.",
		"-",
		"-## Handle cancellation and response loss",
		"-",
		"-Every Operation receives `ctx.signal` and an immutable deadline. Collection",
		"-reads, writes, locks, and dispatch observe both.",
		"-",
		"-Cancellation before commit rolls back all writes. Cancellation or response",
		"-loss after commit cannot undo PostgreSQL state. QUESTPIE reports",
		"-`COMMITTED_RESULT_UNAVAILABLE` with the stable call and transaction identity.",
		"-On Wire v2 this is a framework transaction outcome, not an authored error. It",
		"-uses HTTP `500`, `retryable: true`, the correlated frame `callId`, and canonical",
		"-PostgreSQL transaction identity. Retryability permits exact same-ID recovery;",
		"-the transport does not replay the Mutation automatically.",
		"-",
		"-A Mutation call identity includes the application, Tenant, Operation,",
		"-Principal, and `callId`. It also binds the canonical input digest. The result",
		"-receipt commits with the business change. Retrying the same call returns the",
		"-stored result without another write. Reusing the ID with different input",
		"-fails with the declared `IDEMPOTENCY_CONFLICT` error.",
		"-",
		"-The Runtime mints a `callId` when a direct caller does not provide one. A caller",
		"-that may retry supplies and preserves the same `callId`, as in the Collection",
		"-create example.",
		"-",
		"-Direct and network calls enter the same Operation engine. Direct calls preserve",
		"-typed runtime values such as `Date`. Network calls use the compiled codec to",
		"-encode and restore them. Both paths return the same typed result, declared",
		"-error, nondisclosing not-found result, and transaction outcome.",
		"+The compiler-recognized effect graph admits sequential bounded Collection",
		"+reads/writes and Job acceptance only. A list requires an explicit `first`.",
		"+Bounded `for...of` is legal only over a compiler-known bounded result.",
		"+`Promise.all`, detached work, Services, Actions, Request, Route, filesystem,",
		"+network, timers, raw SQL, and raw transactions are unrepresentable.",
		"+",
		"+Nested lifecycle writes execute synchronously in authored invocation order",
		"+through the same Collection kernel. They share the outer transaction and its",
		"+budgets. Exceeding the compiled re-entry-depth limit, any fault, or cancellation",
		"+rolls back the root write, nested writes, Change Ledger facts, accepted Jobs,",
		"+and result receipt.",
		"+",
		"+`afterWrite` is for a small audit write or durable Job acceptance. An external",
		"+effect belongs to an Action, normally called by an accepted Job after commit.",
		"+There is no `afterRead` phase or general hook catalogue.",
		"+",
		"+## Follow the write order",
		"+",
		"+For each generated Collection write, QUESTPIE:",
		"+",
		"+1. Decodes exact Operation input and enforces Operation admission.",
		"+2. Opens the Mutation transaction and freezes `ctx.now` from PostgreSQL",
		"+   `transaction_timestamp()`.",
		"+3. Decodes caller and trusted lanes, rejects unknown keys and overlap, scopes",
		"+   and locks the current row, and authorizes caller Field paths.",
		"+4. Runs scalar normalization and authored `normalize` separately per lane.",
		"+5. Builds the complete candidate from the create base or locked current row,",
		"+   trusted values, defaults, and nullable SQL `NULL` values.",
		"+6. Runs complete Field/Codec validation and authored `validate`.",
		"+7. Enforces candidate Policy, then runs Policy-aware `check`.",
		"+8. Executes the PostgreSQL write, constraints, and database-owned",
		"+   `onUpdate: \"now\"`, returning the final row.",
		"+9. Runs pre-commit `afterWrite` and all bounded nested work.",
		"+10. Applies output Field authority, validates the output and result receipt,",
		"+    and commits once.",
		"+11. Encodes the result.",
		"+",
		"+`values` bypasses only caller Field authority. The complete candidate still",
		"+passes both validation layers, candidate Policy, and PostgreSQL constraints.",
		"+Policy never supplies or rewrites a value.",
		"+",
		"+## Understand time, retry, and cancellation",
		"+",
		"+`ctx.now` is one transaction-stable `Date` owned by PostgreSQL. Use it when",
		"+several writes describe one business instant. A Field with `onUpdate: \"now\"`",
		"+is stronger: PostgreSQL owns the value, no lane may provide it, and every",
		"+kernel or explicitly supported managed-writer update returns and records the",
		"+final database value.",
		"+",
		"+QUESTPIE does not automatically retry arbitrary Mutation or lifecycle code. A",
		"+pre-commit issue, fault, deadline, limit, or cancellation rolls back. Retrying",
		"+the exact same uncommitted call starts a fresh transaction and reruns lifecycle",
		"+work. Replaying an already committed Call Identity returns the stored receipt",
		"+without rerunning handlers or lifecycle phases.",
		"+",
		"+Cancellation or response loss after commit cannot undo state. QUESTPIE returns",
		"+`COMMITTED_RESULT_UNAVAILABLE` with stable call and transaction identities.",
		"+`retryable: true` permits exact same-call recovery; it does not authorize a",
		"+transport-owned automatic replay.",
		"+",
		"+## Keep the failure classes distinct",
		"+",
		"+| Result                        | Meaning                                        | Public behavior                                                 |",
		"+| ----------------------------- | ---------------------------------------------- | --------------------------------------------------------------- |",
		"+| `null` from keyed read/update | missing or Policy-invisible                    | intentionally indistinguishable                                 |",
		"+| Collection Issue              | expected reusable `validate`/`check` invariant | mapped by the named Mutation after rollback                     |",
		"+| PostgreSQL constraint failure | database race/constraint authority             | typed framework constraint result where declared; no raw detail |",
		"+| unknown fault                 | bug, tampering, unsupported Runtime failure    | rollback plus sanitized `INTERNAL`                              |",
		"+| post-commit ambiguity         | commit may have succeeded but result was lost  | stable `COMMITTED_RESULT_UNAVAILABLE` recovery identity         |",
		" ",
		" Next, [watch Queries in realtime](./realtime).",
		"diff --git a/docs/adr/README.md b/docs/adr/README.md",
		"index d71deb3b4..13c35b698 100644",
		"--- a/docs/adr/README.md",
		"+++ b/docs/adr/README.md",
		"@@ -35,6 +35,8 @@ product models. Git history and `docs/v4/research/` preserve the evidence.",
		" 27. [Simplify V4 Delivery Around Runnable Tracers](./0027-simplify-v4-delivery-flow.md)",
		" 28. [Freeze Action Effect Identity, Limits, and Operation Wire v3](./0028-freeze-action-effect-identity-limits-and-wire-v3.md)",
		" 29. [Unify Policy expression authoring](./0029-unify-policy-expression-authoring.md)",
		"+30. [Freeze Collection provenance and trusted values](./0030-freeze-collection-provenance-and-trusted-values.md)",
		"+31. [Freeze Collection lifecycle programs and issue mapping](./0031-freeze-collection-lifecycle-programs-and-issue-mapping.md)",
		" ",
		" ## Open decisions",
		" ",
		"@@ -81,6 +83,12 @@ existing internal Collection create/update kernel. Trusted values bypass only",
		" caller Field authority; the fully merged candidate still passes the same",
		" Policy, validation, constraint, receipt and transaction boundaries. It does not",
		" publish automatic network CRUD or accept the deferred lifecycle redesign.",
		"+ADR-0031 resolves that lifecycle deferral with exactly `normalize`, `validate`,",
		"+`check`, and `afterWrite`; one canonical compiler-interpreted lifecycle",
		"+program; payloadless Collection issues; explicit Operation-owned issue mapping;",
		"+`ctx.now`; and database-owned `onUpdate: \"now\"`. It preserves Policy as the",
		"+sole authored authorization mechanism, one Mutation transaction, and one",
		"+generated Collection write kernel.",
		" ADR-0021 accepted the connected beta.1 slice: compiler through minimal Studio,",
		" including Service lifetime, watched Query, one committed-fact Reaction, and",
		" explicit absence stories for later breadth. ADR-0024 removes the Studio path",
		"diff --git a/docs/v4/executable-definition-compiler.md b/docs/v4/executable-definition-compiler.md",
		"index 9024f9ce9..1f9a66476 100644",
		"--- a/docs/v4/executable-definition-compiler.md",
		"+++ b/docs/v4/executable-definition-compiler.md",
		"@@ -103,6 +103,13 @@ A value that reaches both graphs must satisfy the structural determinism rules.",
		" An impure or ambiguous shared capture fails with the Definition and captured",
		" declaration Origins.",
		" ",
		"+Collection lifecycle members are not additional executable slots. The compiler",
		"+parses their inline ordinary-TypeScript subset into the canonical Lifecycle",
		"+Program accepted by ADR-0031, then discards the authored callback. Runtime",
		"+interprets only those program bytes. This keeps arbitrary application handlers",
		"+in the Runtime graph while making lifecycle purity, phase capabilities, and",
		"+transitive Collection-issue reachability structurally checkable.",
		"+",
		" ## Output materialization",
		" ",
		" Input comes from the local codec. Context comes from the current App Contract.",
		"diff --git a/docs/v4/lifecycle-jobs-and-shared-durable-kernel.md b/docs/v4/lifecycle-jobs-and-shared-durable-kernel.md",
		"index 267444b8d..b6db678e8 100644",
		"--- a/docs/v4/lifecycle-jobs-and-shared-durable-kernel.md",
		"+++ b/docs/v4/lifecycle-jobs-and-shared-durable-kernel.md",
		"@@ -6,16 +6,18 @@ durable runtime.",
		" ",
		" ## Lifecycle mapping",
		" ",
		"-| V3 job           | V4 owner                                                                                                                           | Forbidden failure mode                                             |",
		"-| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |",
		"-| `beforeValidate` | runtime codec plus closed pure normalization/validation; named Mutation validation for application logic                           | ambient I/O before the write transaction and ambiguous error order |",
		"-| `beforeChange`   | closed server values for ordinary operations; named Mutation for reads, branches, cross-Collection invariants, and declared errors | stale pre-transaction reads or an overpowered callback             |",
		"-| `afterChange`    | Mutation-joined audit/application writes plus exact committed durable dispatch                                                     | external effects in the transaction or crash-lossy in-memory work  |",
		"-| `afterRead`      | selection/output codec, closed pure projection, or named Query in its snapshot                                                     | failure after a committed write that falsely resembles rollback    |",
		"+| V3 job           | V4 owner                                                                                           | Forbidden failure mode                                             |",
		"+| ---------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |",
		"+| `beforeValidate` | runtime codecs plus Collection `normalize` and `validate` Lifecycle Programs                       | ambient I/O before the write transaction and ambiguous error order |",
		"+| `beforeChange`   | trusted values, candidate Policy, and Policy-aware transactional `check`                           | stale pre-transaction reads or an overpowered callback             |",
		"+| `afterChange`    | pre-commit bounded `afterWrite`; Job acceptance or Action for durable/external work                | external effects in the transaction or crash-lossy in-memory work  |",
		"+| `afterRead`      | selection/output codec, closed projection, or named Query; there is no `afterRead` lifecycle phase | failure after a committed write that falsely resembles rollback    |",
		" ",
		" Reaction is therefore not a replacement name for every hook. Pure input work,",
		"-transactional work, result projection, and durable post-commit work retain",
		"-different owners.",
		"+database-backed validation, bounded transactional work, result projection, and",
		"+durable post-commit work retain different owners. Lifecycle TypeScript is",
		"+compiled into a phase-capability-checked canonical program and never invoked as",
		"+arbitrary callback JavaScript.",
		" ",
		" ## One kernel, three meanings",
		" ",
		"diff --git a/docs/v4/query-mutation-and-lifecycle.md b/docs/v4/query-mutation-and-lifecycle.md",
		"index 4c2d429ea..b1f230b15 100644",
		"--- a/docs/v4/query-mutation-and-lifecycle.md",
		"+++ b/docs/v4/query-mutation-and-lifecycle.md",
		"@@ -48,7 +48,7 @@ transaction, Policy-bypass, external-Action, or ordinary System capability.",
		" Every Query owns one bounded consistent read snapshot. The accepted PostgreSQL",
		" choice for a multi-statement semantic Query is `REPEATABLE READ READ ONLY`.",
		" ",
		"-Mutation Context adds exact Policy-aware writes, `ctx.operationTime`,",
		"+Mutation Context adds exact Policy-aware writes, `ctx.now`,",
		" `ctx.callId`, `ctx.transactionId`, and typed `ctx.dispatch`. One Mutation owns",
		" exactly one PostgreSQL transaction. Every generated read and write, complete",
		" candidate check, PostgreSQL Constraint, audit write, result receipt, and",
		"@@ -150,44 +150,71 @@ The internal create/update kernel exists without public exposure. Neither a",
		" Collection nor an input helper publishes an Operation, and this extension does",
		" not add an automatic get/list/delete surface.",
		" ",
		"+## Collection lifecycle programs and issues",
		"+",
		"+A Collection may declare payloadless issues and exactly four lifecycle phases.",
		"+The callbacks use ordinary TypeScript syntax, but the compiler lowers supported",
		"+syntax into one canonical Lifecycle Program; Runtime never calls authored",
		"+lifecycle JavaScript. `normalize` and `validate` have no capabilities. `check`",
		"+adds bounded Policy-aware reads. `afterWrite` adds sequential bounded kernel",
		"+reads/writes and Job acceptance and remains pre-commit. No phase can reach an",
		"+ambient global, import, Service, Action, Request, Route, raw SQL, or raw",
		"+transaction.",
		"+",
		"+`validate` and `check` throw generated Collection Issues. Issues carry no public",
		"+code, status, message, or payload. Each named Mutation structurally maps all",
		"+transitively reachable issues to its own payloadless declared Operation errors.",
		"+The first issue raised in program order dooms the transaction and wins. Mapping",
		"+runs after rollback in the shared Operation engine; an absent or forged mapping",
		"+becomes sanitized `INTERNAL`. Missing and Policy-invisible rows remain",
		"+indistinguishable, and PostgreSQL failures never become Collection Issues.",
		"+",
		" ## Fixed write lifecycle",
		" ",
		" The accepted order is:",
		" ",
		" 1. decode exact Operation input;",
		" 2. enforce Operation admission;",
		"-3. open the Mutation-owned transaction and freeze `operationTime`;",
		"+3. open the Mutation-owned transaction and freeze `now`;",
		" 4. decode exact Collection caller `input` or `patch` and trusted `values`;",
		" 5. reject unknown Collection keys and lane overlap;",
		" 6. apply existing/current row scope and lock;",
		" 7. authorize only canonical caller-supplied Field paths;",
		"-8. normalize supplied caller and trusted Field scalars separately;",
		"+8. normalize supplied caller and trusted Field scalars separately, then run the",
		"+   authored `normalize` program over each lane without changing its paths;",
		" 9. construct create defaults and nullable SQL `NULL`, or start update from the",
		"    locked current row without reapplying create defaults;",
		" 10. overlay normalized trusted `values`;",
		" 11. reject a missing required candidate Field;",
		" 12. validate every Field in the complete candidate against its framework Codec;",
		"-13. enforce candidate Policy;",
		"-14. enforce PostgreSQL Constraints;",
		"-15. select the result;",
		"-16. apply output Field authority;",
		"-17. validate and materialize the output codec;",
		"-18. commit once;",
		"-19. encode the result.",
		"+13. run authored `validate`;",
		"+14. enforce candidate Policy;",
		"+15. run authored Policy-aware `check`;",
		"+16. enforce PostgreSQL Constraints, materialize database-owned `onUpdate`",
		"+    values, and return the written row;",
		"+17. run pre-commit `afterWrite`, including bounded nested kernel work and Job",
		"+    acceptance in authored order;",
		"+18. select the result and apply output Field authority;",
		"+19. validate and materialize the output codec;",
		"+20. write the result receipt and commit once;",
		"+21. encode the result.",
		" ",
		" This candidate validation is framework Field and Codec validation, not an",
		" authored lifecycle callback. Policy decides caller Field authority before",
		" normalization can change a supplied path. A named Mutation may derive trusted values from its input,",
		"-Policy-aware transaction reads and locking reads, `ctx.operationTime`,",
		"+Policy-aware transaction reads and locking reads, `ctx.now`,",
		" `ctx.callId`, and ordinary TypeScript expressions over those values. Runtime",
		"-does not inspect arbitrary TypeScript provenance. `createdAt` and `updatedAt`",
		"-are ordinary Fields. Each trusted assignment and every `updatedAt` change is",
		"-explicit and Mutation-owned.",
		"-",
		"-Use an inline named Mutation when work spans Collections or owns an application",
		"-error, cross-Collection invariant, transactional audit record, or typed",
		"-dispatch intent. External effects belong to a later Action after commit. There",
		"-is no `before*`, `after*`, or general hooks catalogue.",
		"+does not inspect arbitrary Mutation-handler TypeScript provenance. `createdAt`",
		"+remains an ordinary Field. An `updatedAt` Field may instead declare",
		"+`onUpdate: \"now\"`; then PostgreSQL owns it and neither caller nor trusted lane",
		"+may supply it.",
		"+",
		"+Use an inline named Mutation when work spans Collections or owns a public",
		"+application error. Reusable row-local invariants belong to `validate`;",
		"+database-backed invariants belong to `check`; bounded transactional audit and",
		"+Job acceptance may belong to `afterWrite`. External effects belong to an Action",
		"+or accepted Job attempt, never `afterWrite`. There is no `afterRead`, priority",
		"+registry, or general hook catalogue.",
		" ",
		" ## Errors, cancellation, and stable call identity",
		" "
	]
}
