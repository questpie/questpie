{
	"protocolVersion": 2,
	"ticket": "DX-INVERSE-TOMANY-PROJECTION-RATIFICATION",
	"profile": "questpie.acceptance.v2",
	"manifestPath": "docs/v4/prototypes/inverse-tomany-projection/acceptance-manifest.json",
	"reviewedHead": "515689baf836246f95c180bff66d1481f084c1fc",
	"diffBase": "efd835a073ae33e4dfa3f5bef2cf2cad8c46c393",
	"packetDigest": "afa46fdcf09feb12fc86d83e5ae29dcbc9a9594e0a009ebcc4e2638baa850222",
	"primary": {
		"profile": "claude-opus-medium-v1",
		"disposition": "PASS",
		"findings": "VERDICT: PASS\n\n## Blocker disposition (from the preserved `REVIEW.json` at head `9ba3770c8`)\n\n**B1 — unrecorded diagnostic-registry extension: repaired.**\n`docs/adr/0032-…md` now names the registry in its supersession ledger (\"the workbench section 15 claim that the data-diagnostic registry ends at `QP-DATA-014`\"), explicitly registers `QP-DATA-022` and `QP-DATA-026`, and states that `QP-DATA-015`–`021` and `023`–`025` remain unregistered. `authority-projection.json` now amends §15: the closed-registry sentence, `DataDiagnosticCodeV1`, `DataDiagnosticClassV1`, and two new code/class/phase/blocking table rows. Acceptance criterion 13 drops the false \"only ADR-0008\" scope and matches the ledger.\n\n**B2 — unspecified canonical v2 root member: repaired.**\nADR-0032 now states \"The v2 template adds the canonical root member `maximumRelationEdges: 4`\" and that \"Its canonical digest binds the root ceiling and all of those inverse-node facts.\" Criterion 5 enumerates it. The repair was made in authority rather than in the vector, so `v2Vector`, `dcec680988639ba61a9e189cb456f61d2f7718c0f1a0bd79eb3a1e2105e9add9`, and the derived statement/Runtime Build digests are unchanged and remain internally consistent.\n\n**B3 — child-order inference channel: repaired.**\nADR-0032's nested grammar now requires every child order Field to be directly selected and unconditionally visible under selected-output Field Policy, failing compile with `QP-DATA-008`; the outcome table and the projected public guide carry the same rule. `authoring-types.ts` enforces it structurally (`Order extends OrderedField<UnconditionallyVisibleFields<Fields>>` plus `Record<keyof Order, true>` on `select`), and `authoring-types.test.ts` adds both hostiles (unselected `createdAt`, conditionally disclosed `body`). `postgres/proof-kernel.ts` adds `assertChildOrderDisclosure` and the PostgreSQL test proves both refusals occur with `dispatches === 0`.\n\nNo blocking finding remains against the packet's authority, criteria, or evidence.\n\n## Non-blocking observations\n\n1. **`authority-projection.json` pins a stale base.** `base`/`candidate` remain `6e5eb5dc8` and the note asserts the staging commit adds \"proof-only files without changing these selected authority files\", but the reviewed head `515689baf` does change one selected file, `docs/adr/0032-…md`. The projection still applies correctly because its ADR hunk touches only the `Status` line with stable context, and `staging-check.ts` applies it against `HEAD`, not the pinned base — so the post-PASS result is exact. The pin and the note are nevertheless stale.\n2. **`QP-DATA-008`'s projected trigger text is not broadened.** The §15 table row still reads \"An order Field has no direct selected output.\" ADR-0032 assigns the code a second compile trigger (conditionally visible child order Field). Code, class, phase, and blocking are unchanged and ADR-0032 is the governing authority, but the projected row does not restate the new trigger.\n3. **Criterion 10's \"authored filter\" and \"correlation\" tokens remain unevidenced.** `live-query/dependencies.ts` emits no filter token and `changeReachesDependency` ignores filters; relation identity stands in for the correlation endpoint. Behaviour is conservatively over-dirtying, so no correctness claim fails, but the criterion enumerates more than the proof demonstrates. (Carried unchanged from the prior review.)\n4. **`assertChildOrderDisclosure` is unlinked to the artifact.** The new guard is a standalone function; `artifacts/v2.test.ts` does not run it against `v2Vector`'s child order/select sets, so the compile rule and the pinned template are proven separately rather than jointly.\n5. **Cancellation evidence still uses a proxy statement** (`SELECT pg_sleep(30)` rather than `inverseListSql`), Policy is still simulated as `authorized`/`body_allowed` boolean columns, parity is still a `JSON.parse(JSON.stringify(...))` round trip over the same in-process kernel, and `v1Vector`/`recursiveV1Vector` are proof-authored rather than compiler-exported. All are honest for a design ratification and correctly deferred to implementation.\n6. **`authorityHeads` still omits the reviewed head.** The manifest names `efd835a07`, `6e5eb5dc8`, and `fb559ef2f` but not `515689baf`. Git and this record pin it; traceability only."
	},
	"verdict": "PASS",
	"recordedAt": "2026-09-01T09:46:36.235Z"
}
