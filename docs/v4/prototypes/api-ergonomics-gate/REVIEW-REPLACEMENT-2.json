{
	"protocolVersion": 1,
	"ticket": "#301 API ergonomics gate",
	"model": "opus",
	"effort": "medium",
	"reviewedHead": "ff2dfa762c953f2511c5f65e6f930bac3da77868",
	"diffBase": "20ad8529ee18aba6830a7646acb3a9c9292f2fc6",
	"verdict": "PASS",
	"findings": "VERDICT: PASS\n\n## Blocker status\n\nBoth recorded blockers from `REVIEW-REPLACEMENT.json` (`d377482b`) are repaired at `ff2dfa76`, and I found no new blocking finding.\n\n**B1 (off-target amendment anchors) — repaired.** `AMENDMENT.md` now anchors by quoted current text: \"Replace only the paragraph under **Identity invariants** that opens 'A name can be a strict dotted prefix of another name'\". That paragraph is the one carrying \"never a nested-object projection\" in Accepted `docs/v4/definition-composition.md`, so the actual contradiction is the thing replaced. It then explicitly retains the \"Changing `kind` or `name`\" and \"The canonical Compiled Manifest sorts Resources\" paragraphs unchanged, and closes with \"No other paragraph … is deleted or positionally selected.\" The accepted-risk replacement quotes \"Exact full-name key maps are the canonical, collision-free surface\", which matches the current text verbatim.\n\n**B2 (missing `executable-definition-compiler.md` amendment) — repaired.** `AMENDMENT.md` adds a section quoting the exact bullet \"one canonical exact-key generated server member\" under **Collection Operation Set**, replaces it with the identity/capability split, and explicitly retains \"one generated Collection client alias\". It also discharges the reviewer's secondary request by amending Gate 3's \"The generated App Contract uses exact keys, context, input, output, exposure, and declared errors\" bullet in `docs/v4/implementation-gates.md` rather than leaving it ambiguous.\n\n**Registry completeness.** `QP-COMPOSE-023`/`024` are given class, trigger, exit `2`, severity `error`, blocking `fatal`, recovery text, Origin-carrying requirement, and both closed-union appendices — the members the accepted `CompositionDiagnosticV1` envelope requires.\n\n**Prior B2 from `REVIEW.json` (thenable assimilation) remains repaired.** Leaves are frozen callable stand-ins with an invocation counter; non-assimilation is asserted at root, namespace, and callable leaf; `QP-COMPOSE-024` uses `segments.at(-1) === \"then\"`, so `action:delivery.then` is rejected, and `HOSTILE-PROJECTION.json` carries both fixtures.\n\n**Scope.** The diff is confined to `docs/v4/prototypes/api-ergonomics-gate/`. No `packages/**`, no Runtime implementation, and no Accepted authority file is edited — consistent with `AMENDMENT.md`'s pre-PASS projection ban and Gate 10.\n\n**Repairs to previously non-blocking items are real, not cosmetic.** Platform/arch hard-asserts are gone; `Bun.version` and `ts.version` are checked against `MEASUREMENTS.environment`; instantiations are now derived from a live `tsc --extendedDiagnostics` run and compared to the recorded value; `maximumMeasuredDeclarationBytes` is derived and compared; over-length segment (64) and name (5×63 + dots = 319) fixtures now exercise the previously dead validation branches; `beforeValidate` is added to the hook-absence assertion; `unified-generated.ts` / `unified-structural.ts` are now in both `types/tsconfig.json` and the proof's `names` array, so the module-graph claim is compiled rather than only regex-matched.\n\n## Non-blocking observations\n\n- **`AMENDMENT.md` \"Server call examples\" uses \"At minimum this includes:\".** Criterion 8 demands an *exact* amendment for every contradiction. The general rule (\"Replace only generated server capability bracket-key calls with nested paths\") plus the enumeration is workable, but the enumerated files other than `apps/docs/content/docs/v4/durable-reactions.mdx` are not in this packet, so exhaustiveness is unverifiable here. In particular `apps/docs/content/docs/v4/queries-and-mutations.mdx` (linked from the reactions page) is not named, only its design-fiction counterpart. Verify at projection time.\n- **The public MDX prefix paragraph is referenced by description, not quotation** (\"Replace its prefix paragraph with:\" for `apps/docs/content/docs/v4/definition-composition.mdx`), unlike the workbench anchors. The file is not supplied, so the anchor cannot be checked against current text.\n- **Paragraph (3)'s trailing sentence is dropped, not just its contradicting clause.** \"A later generated-client grill can add collision-free ergonomic aliases, but an alias cannot replace or reinterpret the exact-key surface\" disappears in the replacement. The substance survives via \"never replaces or reinterprets canonical Resource Identity\" plus the retained client-alias bullet, but this is a normative deletion the gate does not claim to make.\n- **The hostile-collision-matrix replacement is identified by role, not by quoted row text** (\"Replace the hostile-collision row with:\"). The target row is unique, so it resolves, but it is the one reference in that file not anchored the way the section's own closing sentence promises.\n- **Recorded editor p95 values remain a stale sample.** `run.mjs` compares `measurements.editor.*` only against `budgetP95Milliseconds` and `depth`; it never compares them to the live `typeScript.stress[size].completionP95Milliseconds`. The live gate does exist inside `measureStress` (`assert.ok(p95 <= 100)`), so the budget is enforced — but the committed numbers are not tied to the current run. Same for `focusedRunner.wallMilliseconds: 2670`.\n- **Declaration bytes remain a fixture proxy.** `noEmit: true`; `maximumMeasuredBytes: 84473` is `stat()` of hand-written `generated-app.ts` + `generated-package.ts` plus the 500-Operation stress source. Disclosed in `MEASUREMENTS.environment.fixture`, and conservative, but not emitted `.d.ts` bytes.\n- **Discovery-order independence is by construction.** `buildOperationProjection` pre-sorts by ASCII, so a prefix always precedes its extension; the removal of the descendant branch is sound only because of that sort. `reverseCollision` re-sorts to the identical sequence, so `stableAcrossDiscoveryOrder: true` reflects the sort, not an ordering experiment that could have failed.\n- **Hostile Origins are committed strings**, not resolved from a source fixture, so \"both Origins\" is asserted at fixture level.\n- **`assert.equal(measuredInstantiations, 1594)` is exact, not a budget check.** With Bun and TypeScript versions asserted this is deterministic, but a pinned-toolchain bump will surface as a hard failure rather than reported budget movement.\n- **`hostileCases` and `durableKernel`/`durableAuthoring` are declarative.** The underlying behaviors are exercised elsewhere in the runner and in `types/negative.ts`, but the emitted result object reads as measurement.\n- **Null-prototype capability maps have an ergonomic consequence worth recording at projection time**: with `action:a.toString.b` present, `actions.a.toString` is a namespace object and the map has no inherited `toString`, so string coercion of a capability namespace throws. The proof treats this as safe (and it is, for the tested paths), but it belongs in the projected contract text.\n- **Pre-existing, outside this diff:** `docs/v4/definition-composition.md` states \"exactly seven pure current-virtual factory values\" and then lists six (`defineWorkflow` missing). Not introduced or claimed by this gate, but the ADR/projection step named in `AMENDMENT.md` is a natural place to correct it.",
	"recordedAt": "2026-08-14T19:50:59.227Z"
}
