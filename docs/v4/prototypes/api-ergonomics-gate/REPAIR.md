# Acceptance-review repair

Initial reviewed head `cb0a73d6b13cd4481d2572b88ecf829816d576aa`
received a valid fresh stateless Claude Opus-medium `BLOCKED` verdict recorded
verbatim in `REVIEW.json`.

- B1 is repaired by `AMENDMENT.md`: it enumerates the exact Accepted workbench,
  public page, diagnostic registry, union, envelope, recovery, grammar
  restriction, collision-matrix, example, ADR, and HANDOFF changes that must be
  projected together only after PASS. `DECISION.md` and `ACCEPTANCE.json` now
  bind that explicit contradiction resolution.
- B2 is repaired by using frozen callable Operation stand-ins, proving that
  resolving the root, a nested namespace, or a callable leaf invokes no
  Operation, rejecting a final `then` segment at every depth, and sourcing the
  root and nested hostile fixtures plus Origins from
  `HOSTILE-PROJECTION.json`.

The repair also records and asserts Bun, TypeScript, platform, and architecture;
uses portable URL-to-path conversion; and binds hostile Origins to committed
fixture data.
