{
	"protocolVersion": 1,
	"ticket": "#14 conformance applications and hostile matrices",
	"model": "opus",
	"effort": "medium",
	"reviewedHead": "56a39c27704afef00c6b25fdbd13ade88278b668",
	"diffBase": "b5b5073df8168f6a551cae000f1b7286cae199f8",
	"verdict": "PASS",
	"findings": "VERDICT: PASS\n\n## Blocker resolution (from the recorded `e222be74` BLOCKED review)\n\n**1. False quality gate in `README.md` — closed.** `check.ts` now parses `MATRIX.md` into structured rows and enforces what the README asserts:\n\n- Omissions: 7-column arity with no empty cell (`incomplete matrix row`), duplicate-cell rejection, and all 20 `requiredCells` present.\n- Single-domain: `rows.filter(row[2] === \"both\").length < 12` — a matrix mention in the prose Fixtures section no longer satisfies it. I confirmed the committed matrix has 16 `both` rows against the threshold of 12.\n- Forbidden optional-authority/RLS claims: two negative regexes plus an exact `accelerators` owner check (`Runtime capability bindings`).\n\n`negative-control.ts` supplies five disposable mutations (missing row, empty column, single domain, optional authority, RLS claim) and I traced each against the committed `MATRIX.md` text: every mutation regex matches its intended target and produces a `validate` throw. The controls also fail loudly rather than silently if a mutation stops matching, since a non-matching `replace` leaves the source valid and trips `negative control passed: …`.\n\n**2. Unowned Channel Resource semantics — closed.** A `channel` row exists with owner `compiler, Policy, and PostgreSQL`, both fixtures, direct-publish/SSE-POST/worker/Studio surfaces, and hostile cases covering codec failure, denied publish/subscribe, changed-payload conflict, subject collision, order, bounded replay/gap, authority reset, and limits — matching Gate 8C and ADR-0019. `accelerators` retains `Channel carrier` as carrier-only, and the cross-product rule states `channelCarrier` is transport only. `check.ts` asserts both the Channel owner triple and the `changed-payload` hostile case, and `channel` is in `requiredCells`.\n\n## Other criteria checked\n\n- Authority heads in `ACCEPTANCE.json` match the `HANDOFF.md` table; `projectionSnapshot` matches the supplied diff base `b5b5073d`.\n- No production Runtime code: the diff is five files under `docs/v4/prototypes/conformance-p14/`.\n- No RLS claim; Index stays B-tree-only; PostgreSQL remains durable authority in every row.\n- V3 routing section reuses behavioral evidence and explicitly refuses the v3 test tree, serial loop, provider matrices, Admin backend, and hook catalogue.\n- Commands are repository-portable (`bun`, `bunx oxlint`, `bunx oxfmt`, `git diff --check`); no personal skill path or agent-local dependency.\n- Prior non-blocking observations were all addressed: Seed dependency/order/replay hostile case and `Migration/Seed receipts` artifact; `managed-postgres` lane moved from `manual` to the accepted `release` lane; explicit archive-scope rationale; explicit Action deferral bullet; Oxfmt recorded in the manifest.\n\n## Non-blocking observations\n\n- **Gate 8A has no single named owner row.** Readiness refusal, bounded drain, Runtime Build pairing, and wire/executable/realtime compatibility retirement are distributed across `definitions`, `service-route-auth`, and `ha` rather than owned by one cell. This is consistent with `ACCEPTANCE.json` criterion 2, which does not enumerate Runtime lifecycle, but #15/#16 slicing will need an explicit owner for those assertions.\n- **Gate 8 maintenance commands are not visible.** `acknowledgeAmbiguity`, `cancelRun`, `drainRuntime`, and `retryRun` require maintenance Authority, expected-version fencing, a typed winner, and append-only audit; the `envelope` and `durable` rows do not name that hostile case.\n- **The `12` threshold in the single-domain gate is a magic constant.** It is defensible at 16/20 today, but a later matrix that drops archive from four more rows would still pass. Consider expressing it as a required fraction or an explicit per-cell fixture allowlist.\n- **`negative-control.ts` is not covered by the recorded scoped Oxlint run**; `ACCEPTANCE.json` lints only `check.ts`. Oxfmt does cover the directory.\n- **`ACCEPTANCE.json` indentation** is tabs in the committed diff. Oxfmt's JSON coverage is worth confirming so the recorded format check genuinely governs this file rather than only the two TypeScript files.",
	"recordedAt": "2026-08-13T23:37:48.950Z"
}
